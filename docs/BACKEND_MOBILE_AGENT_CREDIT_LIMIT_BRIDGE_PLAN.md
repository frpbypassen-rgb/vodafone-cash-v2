# خطة تنفيذ Backend Mobile Bridge - الوكيل والحسابات التابعة والحد التأميني

هذا الملف موجه لموديل/مطور الباك إند فقط. المطلوب ليس اختراع نظام مالي جديد، بل تعريض المنطق الموجود فعلياً في الباك إند والويب لتطبيق Flutter من خلال Mobile API آمن ومنظم تحت `/api/mobile`.

نسخة التسليم الأساسية موجودة في:

```text
delivery/BACKEND_MOBILE_AGENT_CREDIT_LIMIT_BRIDGE_PLAN.md
```

يجب تنفيذ نفس محتوى نسخة `delivery` حرفياً، وهي المصدر المعتمد لهذه الخطة.
