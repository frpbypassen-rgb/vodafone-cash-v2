<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>حسابات التنفيذ | الأهرام</title>
    
    <script>
        const savedTheme = localStorage.getItem('ahram_theme') || 'dark';
        document.documentElement.setAttribute('data-theme', savedTheme);
    </script>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.rtl.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;500;700;800;900&family=JetBrains+Mono:wght@700&display=swap" rel="stylesheet">
    <script>
        const csrfToken = <%- JSON.stringify(typeof csrfToken !== 'undefined' ? csrfToken : '') %>;
    </script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    
    <style>
        /* =========================================
           ✨ 3D Modern Cards - التصميم ثلاثي الأبعاد
           ========================================= */
        .card-3d-wrapper {
            perspective: 1000px;
            height: 100%;
        }

        .bot-card-3d {
            background: var(--card-bg);
            border-radius: 24px;
            padding: 25px;
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            height: 100%;
            position: relative;
            overflow: hidden;
            border: 1px solid var(--border-glass);
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.05);
            z-index: 1;
        }

        /* توهج خلفي (Glass Glow) */
        .bot-card-3d::before {
            content: '';
            position: absolute;
            top: -50%;
            right: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(255,255,255,0.05) 0%, transparent 60%);
            transform: rotate(45deg);
            pointer-events: none;
            z-index: -1;
        }

        [data-theme="dark"] .bot-card-3d {
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.4);
        }

        /* 🚀 تأثير الرفع والدوران (Tilt) */
        .bot-card-3d:hover {
            transform: translateY(-10px) rotateX(2deg) rotateY(-2deg);
            box-shadow: 0 25px 50px rgba(0, 0, 0, 0.15);
        }

        [data-theme="dark"] .bot-card-3d:hover {
            box-shadow: 0 25px 50px rgba(0, 0, 0, 0.6), inset 0 0 0 1px rgba(255, 255, 255, 0.1);
        }

        /* 🌟 الأيقونة المتوهجة */
        .bot-icon-3d {
            width: 65px;
            height: 65px;
            border-radius: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 30px;
            color: white;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
            position: relative;
        }

        .bot-icon-3d::after {
            content: '';
            position: absolute;
            width: 100%;
            height: 100%;
            border-radius: inherit;
            background: inherit;
            filter: blur(12px);
            z-index: -1;
            opacity: 0.6;
            transition: 0.3s;
        }

        .bot-card-3d:hover .bot-icon-3d::after {
            filter: blur(18px);
            opacity: 0.9;
        }

        .manager-bot-icon { background: linear-gradient(135deg, #3b82f6, #2563eb); }
        .normal-bot-icon { background: linear-gradient(135deg, #10b981, #059669); }
        .api-bot-icon { background: linear-gradient(135deg, #f59e0b, #d97706); }

        /* 💰 مربع الرصيد العميق */
        .balance-box {
            background: var(--input-bg);
            border-radius: 20px;
            padding: 20px;
            margin: 25px 0;
            border: 1px solid var(--border-glass);
            position: relative;
            overflow: hidden;
            box-shadow: inset 0 4px 10px rgba(0,0,0,0.02);
        }

        [data-theme="dark"] .balance-box {
            box-shadow: inset 0 4px 10px rgba(0,0,0,0.2);
        }

        .balance-box::after {
            content: '\f0d6';
            font-family: 'Font Awesome 6 Free';
            font-weight: 900;
            position: absolute;
            left: -15px;
            bottom: -20px;
            font-size: 90px;
            opacity: 0.04;
            transform: rotate(-15deg);
            color: var(--text-main);
            pointer-events: none;
        }

        .mono-num { font-family: 'JetBrains Mono', monospace; font-size: 2.2rem; font-weight: 900; line-height: 1; }

        /* 🔘 الزر التفاعلي */
        .btn-3d {
            background: var(--input-bg);
            border: 1px solid var(--border-glass);
            color: var(--text-main);
            border-radius: 16px;
            padding: 14px;
            font-weight: 800;
            transition: all 0.3s;
            box-shadow: 0 4px 6px rgba(0,0,0,0.02);
            display: flex;
            align-items: center;
            justify-content: center;
            text-decoration: none;
        }

        .btn-3d:hover {
            background: var(--brand-primary);
            color: white;
            transform: translateY(-3px);
            box-shadow: 0 10px 25px rgba(0, 26, 77, 0.3);
            border-color: var(--brand-primary);
        }

        [data-theme="dark"] .btn-3d:hover {
            background: var(--brand-gold);
            color: black;
            border-color: var(--brand-gold);
            box-shadow: 0 10px 25px rgba(212, 175, 55, 0.3);
        }

        .executor-access-bar {
            display: grid;
            grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
            gap: 14px;
            padding: 18px;
            margin-bottom: 18px;
            background: var(--input-bg);
            border: 1px solid var(--border-glass);
            border-radius: 8px;
        }
        .executor-link-label { color: var(--text-muted); font-size: 0.78rem; font-weight: 800; margin-bottom: 7px; }
        .executor-link-input { min-width: 0; direction: ltr; text-align: left; font-family: 'JetBrains Mono', monospace; font-size: 0.78rem; }
        .copy-link-btn { width: 44px; flex: 0 0 44px; }
        .executor-tabs {
            display: inline-flex;
            gap: 4px;
            padding: 4px;
            margin-top: 6px;
            background: var(--input-bg);
            border: 1px solid var(--border-glass);
            border-radius: 8px;
        }
        .executor-tab {
            min-height: 42px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            padding: 0 16px;
            color: var(--text-muted);
            background: transparent;
            border: 0;
            border-radius: 6px;
            font-size: 0.84rem;
            font-weight: 900;
        }
        .executor-tab.active {
            color: #fff;
            background: var(--brand-primary);
            box-shadow: 0 4px 12px rgba(0, 26, 77, 0.2);
        }
        .executor-tab .tab-count {
            min-width: 24px;
            height: 24px;
            display: inline-grid;
            place-items: center;
            padding: 0 6px;
            color: inherit;
            background: rgba(127, 127, 127, 0.16);
            border-radius: 12px;
            font-family: 'JetBrains Mono', monospace;
            font-size: 0.7rem;
        }
        .executor-archive-btn {
            width: 40px;
            height: 40px;
            display: inline-grid;
            place-items: center;
            flex: 0 0 40px;
            padding: 0;
            color: #dc2626;
            background: rgba(220, 38, 38, 0.08);
            border: 1px solid rgba(220, 38, 38, 0.28);
            border-radius: 6px;
            cursor: pointer;
        }
        .executor-archive-btn:hover,
        .executor-archive-btn:focus-visible {
            color: #fff;
            background: #dc2626;
            outline: none;
        }
        .executor-archive-btn.is-locked {
            color: var(--text-muted);
            background: var(--input-bg);
            border-color: var(--border-glass);
        }
        .archive-card {
            height: 100%;
            padding: 20px;
            background: var(--card-bg);
            border: 1px solid var(--border-glass);
            border-right: 4px solid #64748b;
            border-radius: 8px;
            box-shadow: var(--shadow-soft);
        }
        .archive-card-header {
            display: flex;
            align-items: flex-start;
            justify-content: space-between;
            gap: 14px;
        }
        .archive-identity { display: flex; align-items: center; gap: 12px; min-width: 0; }
        .archive-icon {
            width: 48px;
            height: 48px;
            display: grid;
            place-items: center;
            flex: 0 0 48px;
            color: #64748b;
            background: rgba(100, 116, 139, 0.12);
            border-radius: 6px;
            font-size: 1.1rem;
        }
        .archive-metrics {
            display: grid;
            grid-template-columns: repeat(3, minmax(0, 1fr));
            margin: 18px 0;
            padding: 14px 0;
            border-top: 1px solid var(--border-glass);
            border-bottom: 1px solid var(--border-glass);
        }
        .archive-metric { min-width: 0; padding: 0 10px; text-align: center; }
        .archive-metric + .archive-metric { border-right: 1px solid var(--border-glass); }
        .archive-metric span { display: block; color: var(--text-muted); font-size: 0.68rem; font-weight: 800; }
        .archive-metric strong { display: block; margin-top: 5px; color: var(--text-main); font-size: 0.86rem; overflow-wrap: anywhere; }
        .archive-reason {
            min-height: 44px;
            margin-bottom: 15px;
            color: var(--text-muted);
            font-size: 0.78rem;
            font-weight: 700;
            line-height: 1.7;
        }
        .archive-meta { color: var(--text-muted); font-size: 0.72rem; font-weight: 700; }
        .archive-open-btn {
            min-height: 42px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            width: 100%;
            margin-top: 14px;
            color: var(--text-main);
            background: var(--input-bg);
            border: 1px solid var(--border-glass);
            border-radius: 6px;
            text-decoration: none;
            font-size: 0.8rem;
            font-weight: 900;
        }
        .archive-open-btn:hover { color: #fff; background: #475569; border-color: #475569; }
        @media (max-width: 767px) {
            .executor-access-bar { grid-template-columns: 1fr; padding: 14px; }
            .admin-nav { align-items: stretch !important; gap: 12px; }
            .admin-nav .btn { width: 100%; }
            .executor-tabs { width: 100%; }
            .executor-tab { flex: 1; padding: 0 10px; }
            .archive-metric { padding: 0 5px; }
        }
    </style>

    <!-- PWA Setup -->
    <link rel="manifest" href="/manifest.json">
    <meta name="theme-color" content="#001a4d">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <meta name="apple-mobile-web-app-title" content="Al-Ahram">
    <!-- End PWA Setup -->
</head>
<body>

<button class="mobile-nav-toggle" onclick="toggleSidebar()"><i class="fa-solid fa-bars"></i></button>
<div class="sidebar-overlay" onclick="toggleSidebar()"></div>

<%- include('partials/sidebar', { activePage: 'executors' }) %>

<div class="main-content">
    <div class="container-fluid">
        <div class="admin-nav mb-4">
            <h3 class="fw-bold m-0 text-dark"><i class="fa-solid fa-network-wired text-primary me-2"></i> منفذو العمليات</h3>
            <button type="button" class="btn btn-primary fw-bold shadow-sm px-4" data-bs-toggle="modal" data-bs-target="#createExecutorModal"><i class="fa-solid fa-user-plus me-1"></i> إضافة منفذ يدوياً</button>
        </div>

        <div class="executor-access-bar" aria-label="روابط المنفذين">
            <div>
                <div class="executor-link-label"><i class="fa-solid fa-user-pen me-1"></i> رابط تقديم طلب منفذ جديد</div>
                <div class="input-group">
                    <input id="executorRegistrationUrl" class="form-control executor-link-input" value="<%= executorRegistrationUrl %>" readonly aria-label="رابط تسجيل المنفذين">
                    <a class="btn btn-outline-primary copy-link-btn" href="<%= executorRegistrationUrl %>" target="_blank" rel="noopener" title="فتح رابط التسجيل"><i class="fa-solid fa-arrow-up-right-from-square"></i></a>
                    <button type="button" class="btn btn-primary copy-link-btn" onclick="copyExecutorLink('executorRegistrationUrl', 'تم نسخ رابط التسجيل')" title="نسخ رابط التسجيل"><i class="fa-regular fa-copy"></i></button>
                </div>
            </div>
            <div>
                <div class="executor-link-label"><i class="fa-solid fa-right-to-bracket me-1"></i> رابط دخول المنفذين بعد التفعيل</div>
                <div class="input-group">
                    <input id="executorLoginUrl" class="form-control executor-link-input" value="<%= executorLoginUrl %>" readonly aria-label="رابط دخول المنفذين">
                    <a class="btn btn-outline-primary copy-link-btn" href="<%= executorLoginUrl %>" target="_blank" rel="noopener" title="فتح رابط الدخول"><i class="fa-solid fa-arrow-up-right-from-square"></i></a>
                    <button type="button" class="btn btn-primary copy-link-btn" onclick="copyExecutorLink('executorLoginUrl', 'تم نسخ رابط الدخول')" title="نسخ رابط الدخول"><i class="fa-regular fa-copy"></i></button>
                </div>
            </div>
        </div>

        <% if (typeof query !== 'undefined' && query.created) { %>
            <div class="alert alert-success fw-bold rounded-4 border-0 shadow-sm">
                <i class="fa-solid fa-check-circle me-1"></i> تم إنشاء منفذ التنفيذ بنجاح.
            </div>
        <% } else if (typeof query !== 'undefined' && query.createError) {
            const createErrors = {
                MISSING_NAME: 'اسم المنفذ مطلوب.',
                MISSING_LOGIN: 'بيانات دخول مسؤول المنفذ مطلوبة.',
                INVALID_MANAGER_NAME: 'يرجى إدخال اسم مسؤول المنفذ كاملاً.',
                INVALID_USERNAME: 'اسم الدخول غير صالح. استخدم حروفاً إنجليزية وأرقاماً فقط.',
                USERNAME_TAKEN: 'اسم الدخول مستخدم بالفعل.',
                INVALID_PHONE: 'رقم الهاتف غير صالح.',
                WEAK_PASSWORD: 'كلمة المرور يجب ألا تقل عن 6 أحرف.',
                INVALID_BALANCE: 'الرصيد الافتتاحي غير صالح.'
            };
        %>
            <div class="alert alert-danger fw-bold rounded-4 border-0 shadow-sm">
                <i class="fa-solid fa-triangle-exclamation me-1"></i> <%= createErrors[query.createError] || 'تعذر إنشاء المنفذ. تأكد من البيانات وحاول مرة أخرى.' %>
            </div>
        <% } %>

        <% if (typeof query !== 'undefined' && query.archived) { %>
            <div class="alert alert-success fw-bold border-0 shadow-sm" style="border-radius:8px;">
                <i class="fa-solid fa-box-archive me-1"></i> تم نقل حساب المنفذ إلى الأرشيف وحفظ جميع عملياته.
            </div>
        <% } %>
        <% if (typeof query !== 'undefined' && query.serviceUpdated) { %>
            <div class="alert alert-success fw-bold border-0 shadow-sm" style="border-radius:8px;">
                <i class="fa-solid fa-circle-check me-1"></i> تم تحديث خدمة المنفذ. أعد اختياره من إعدادات التوجيه التلقائي للخدمة الجديدة.
            </div>
        <% } %>
        <% if (typeof query !== 'undefined' && query.serviceError) { %>
            <div class="alert alert-danger fw-bold border-0 shadow-sm" style="border-radius:8px;">
                <i class="fa-solid fa-triangle-exclamation me-1"></i> <%= query.serviceError === 'ACTIVE_TASKS' ? 'لا يمكن تغيير الخدمة بينما توجد عمليات قيد التنفيذ لدى هذا المنفذ.' : 'تعذر تحديث خدمة المنفذ.' %>
            </div>
        <% } %>
        <% if (typeof query !== 'undefined' && query.archiveError === 'READ_ONLY') { %>
            <div class="alert alert-warning fw-bold border-0 shadow-sm" style="border-radius:8px;">
                <i class="fa-solid fa-lock me-1"></i> الحساب المؤرشف متاح للقراءة فقط ولا يقبل تعديلات تشغيلية.
            </div>
        <% } %>

        <% const showArchiveTab = typeof query !== 'undefined' && query.tab === 'archive'; %>
        <div class="executor-tabs" role="tablist" aria-label="أقسام حسابات المنفذين">
            <button class="executor-tab <%= showArchiveTab ? '' : 'active' %>" id="current-executors-tab" data-bs-toggle="tab" data-bs-target="#current-executors-pane" type="button" role="tab" aria-controls="current-executors-pane" aria-selected="<%= showArchiveTab ? 'false' : 'true' %>">
                <i class="fa-solid fa-signal"></i> المنفذون الحاليون <span class="tab-count"><%= bots.length %></span>
            </button>
            <button class="executor-tab <%= showArchiveTab ? 'active' : '' %>" id="archived-executors-tab" data-bs-toggle="tab" data-bs-target="#archived-executors-pane" type="button" role="tab" aria-controls="archived-executors-pane" aria-selected="<%= showArchiveTab ? 'true' : 'false' %>">
                <i class="fa-solid fa-box-archive"></i> الأرشيف <span class="tab-count"><%= archivedBots.length %></span>
            </button>
        </div>

        <div class="tab-content pb-5">
            <section class="tab-pane fade <%= showArchiveTab ? '' : 'show active' %>" id="current-executors-pane" role="tabpanel" aria-labelledby="current-executors-tab" tabindex="0">
                <div class="row g-4 mt-2">
                    <% if (bots.length === 0) { %>
                        <div class="col-12 text-center py-5">
                            <i class="fa-solid fa-robot fa-4x text-muted opacity-25 mb-3"></i>
                            <h5 class="text-muted fw-bold">لا توجد حسابات منفذين حالية.</h5>
                        </div>
                    <% } %>

                    <% bots.forEach(bot => { %>
                        <div class="col-xl-4 col-md-6">
                            <div class="card-3d-wrapper">
                                <div class="bot-card-3d" style="border-top: 5px solid <%= bot.isApiBot ? '#f59e0b' : (bot.isManagerBot ? '#3b82f6' : '#10b981') %>;">
                                    <div>
                                        <div class="d-flex justify-content-between align-items-start gap-2 mb-3">
                                            <div class="d-flex align-items-center gap-3 min-w-0">
                                                <div class="bot-icon-3d <%= bot.isApiBot ? 'api-bot-icon' : (bot.isManagerBot ? 'manager-bot-icon' : 'normal-bot-icon') %>">
                                                    <i class="fa-solid <%= bot.isApiBot ? 'fa-plug-circle-bolt' : (bot.isManagerBot ? 'fa-building' : 'fa-robot') %>"></i>
                                                </div>
                                                <div class="min-w-0">
                                                    <h5 class="fw-bold m-0 text-dark mb-1 text-break"><%= bot.name %></h5>
                                                    <span class="badge <%= bot.status === 'active' ? 'bg-success text-success border-success' : 'bg-danger text-danger border-danger' %> bg-opacity-10 border rounded-pill px-3 py-1 mt-1" style="font-size:0.75rem;">
                                                        <i class="fa-solid <%= bot.status === 'active' ? 'fa-check-circle' : 'fa-ban' %> me-1"></i><%= bot.status === 'active' ? 'مفعل ونشط' : 'متوقف' %>
                                                    </span>
                                                    <div class="text-primary small fw-bold mt-2"><i class="fa-solid fa-layer-group me-1"></i><%= bot.serviceLabel %></div>
                                                    <% if (bot.isApiBot) { %>
                                                        <div class="text-muted small fw-bold mt-2"><i class="fa-solid fa-plug me-1"></i><%= bot.apiProviderKey === 'zayn_external_aggregator' ? 'Zayn External Aggregator' : (bot.apiProviderKey || 'API Provider') %></div>
                                                        <% if (bot.lastApiTestAt) { %>
                                                            <div class="small mt-2 <%= bot.lastApiTestStatus === 'success' ? 'text-success' : 'text-danger' %>">
                                                                <i class="fa-solid <%= bot.lastApiTestStatus === 'success' ? 'fa-circle-check' : 'fa-triangle-exclamation' %> me-1"></i>آخر اختبار: <%= new Date(bot.lastApiTestAt).toLocaleString('ar-LY', { timeZone: 'Africa/Tripoli' }) %>
                                                            </div>
                                                        <% } %>
                                                    <% } %>
                                                </div>
                                            </div>
                                            <% if (isMaster) { %>
                                                <button type="button" class="executor-archive-btn <%= bot.status === 'active' ? 'is-locked' : '' %>" data-executor-id="<%= bot._id %>" data-executor-name="<%= bot.name %>" data-executor-status="<%= bot.status %>" onclick="archiveExecutor(this)" title="<%= bot.status === 'active' ? 'أوقف المنفذ أولاً قبل حذفه' : 'حذف الحساب ونقله إلى الأرشيف' %>" aria-label="حذف حساب <%= bot.name %>">
                                                    <i class="fa-solid fa-box-archive"></i>
                                                </button>
                                            <% } %>
                                        </div>

                                        <div class="balance-box text-center">
                                            <div class="text-muted small fw-bold mb-2"><i class="fa-solid fa-wallet me-1 opacity-50"></i> الرصيد / المديونية الحالية</div>
                                            <div class="mono-num <%= bot.balance < 0 ? 'text-danger' : 'text-success' %>" dir="ltr"><%= bot.balance.toFixed(2) %> <span class="fs-6 text-muted">EGP</span></div>
                                        </div>
                                    </div>

                                    <div>
                                        <% if (isMaster && !bot.isManagerBot) { %>
                                            <form action="/executor/<%= bot._id %>/service" method="POST" class="input-group mb-2" onsubmit="return confirm('سيتم إزالة تعيينات التوجيه التلقائي القديمة لهذا المنفذ. متابعة؟');">
                                                <span class="input-group-text" title="خدمة المنفذ"><i class="fa-solid fa-layer-group"></i></span>
                                                <select name="serviceKey" class="form-select fw-bold" aria-label="خدمة المنفذ">
                                                    <% (executorServiceOptions || []).forEach((service) => { %>
                                                        <option value="<%= service.key %>" <%= bot.serviceKey === service.key ? 'selected' : '' %>><%= service.label %></option>
                                                    <% }) %>
                                                </select>
                                                <button type="submit" class="btn btn-primary" title="حفظ خدمة المنفذ" aria-label="حفظ خدمة المنفذ"><i class="fa-solid fa-floppy-disk"></i></button>
                                            </form>
                                        <% } %>
                                        <div class="d-flex justify-content-between align-items-center p-3 rounded-4 mb-3 border" style="background: rgba(0,0,0,0.02);">
                                            <span class="fw-bold small text-muted"><i class="fa-solid fa-check-double me-1 opacity-50"></i> إجمالي العمليات المنجزة</span>
                                            <span class="badge bg-primary fs-6 rounded-pill px-3 shadow-sm"><%= bot.txCount %> طلب</span>
                                        </div>
                                        <% if (bot.isApiBot) { %>
                                            <button type="button" class="btn-3d w-100 mb-2 api-test-btn" data-api-bot-id="<%= bot._id %>" onclick="testApiExecutor(this)"><i class="fa-solid fa-signal me-2"></i> اختبار اتصال API والرصيد</button>
                                        <% } %>
                                        <% if (isMaster) { %>
                                            <a href="/admin/accounts/executor/<%= bot._id %>/edit" class="btn-3d w-100 mb-2" style="color:#087f74; border:1px solid rgba(8,127,116,.35);"><i class="fa-solid fa-pen-to-square me-2"></i> تعديل بيانات المنفذ</a>
                                        <% } %>
                                        <a href="/executor/<%= bot._id %>" class="btn-3d w-100"><i class="fa-solid fa-folder-open me-2"></i> إدارة التفاصيل والملف المحاسبي</a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    <% }) %>
                </div>
            </section>

            <section class="tab-pane fade <%= showArchiveTab ? 'show active' : '' %>" id="archived-executors-pane" role="tabpanel" aria-labelledby="archived-executors-tab" tabindex="0">
                <div class="row g-3 mt-2">
                    <% if (archivedBots.length === 0) { %>
                        <div class="col-12 text-center py-5">
                            <i class="fa-solid fa-box-open fa-4x text-muted opacity-25 mb-3"></i>
                            <h5 class="text-muted fw-bold">أرشيف المنفذين فارغ.</h5>
                        </div>
                    <% } %>
                    <% archivedBots.forEach(bot => { %>
                        <div class="col-xl-4 col-md-6">
                            <article class="archive-card">
                                <div class="archive-card-header">
                                    <div class="archive-identity">
                                        <div class="archive-icon"><i class="fa-solid fa-box-archive"></i></div>
                                        <div class="min-w-0">
                                            <h5 class="fw-bold text-dark m-0 text-break"><%= bot.name %></h5>
                                            <div class="text-muted small mt-1"><%= bot.isApiBot ? 'منفذ API' : (bot.isManagerBot ? 'وكالة إدارية' : 'منفذ يدوي') %></div>
                                        </div>
                                    </div>
                                    <span class="badge bg-secondary bg-opacity-10 text-secondary border border-secondary">مؤرشف</span>
                                </div>
                                <div class="archive-metrics">
                                    <div class="archive-metric"><span>العمليات</span><strong><%= bot.txCount || 0 %></strong></div>
                                    <div class="archive-metric"><span>الحسابات</span><strong><%= bot.archiveEmployeeCount || 0 %></strong></div>
                                    <div class="archive-metric"><span>الرصيد الختامي</span><strong dir="ltr"><%= Number(bot.balance || 0).toFixed(2) %></strong></div>
                                </div>
                                <div class="archive-reason"><i class="fa-solid fa-note-sticky me-1"></i><%= bot.archiveReason || 'لم تسجل ملاحظة للأرشفة.' %></div>
                                <div class="archive-meta">
                                    <i class="fa-regular fa-calendar me-1"></i><%= bot.archivedAt ? new Date(bot.archivedAt).toLocaleString('ar-LY', { timeZone: 'Africa/Tripoli' }) : '---' %>
                                    <span class="mx-1">•</span><%= bot.archivedBy || 'الإدارة' %>
                                </div>
                                <a class="archive-open-btn" href="/executor/<%= bot._id %>"><i class="fa-solid fa-clock-rotate-left"></i> فتح العمليات المحفوظة</a>
                            </article>
                        </div>
                    <% }) %>
                </div>
            </section>
        </div>
    </div>
</div>

<div class="modal fade" id="createExecutorModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-lg modal-dialog-centered">
        <div class="modal-content border-0 shadow-lg" style="background: var(--card-bg); color: var(--text-main); border-radius: 18px;">
            <form action="/executors/add" method="POST">
                <div class="modal-header border-0 pb-0">
                    <h5 class="modal-title fw-bold">
                    <i class="fa-solid fa-user-plus text-primary me-2"></i> إضافة منفذ تنفيذ يدوي
                    </h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <div class="row g-3">
                        <div class="col-md-3">
                            <label class="form-label fw-bold small text-muted">اسم المنفذ</label>
                            <input type="text" name="name" class="form-control fw-bold" required placeholder="مثال: منفذ طرابلس 1" autocomplete="organization">
                        </div>
                        <div class="col-md-3">
                            <label class="form-label fw-bold small text-muted">نوع المنفذ</label>
                            <select name="botType" id="botType" class="form-select fw-bold">
                                <option value="normal">منفذ يدوي</option>
                                <option value="manager">مجموعة إشراف</option>
                                <option value="api">منفذ API</option>
                            </select>
                        </div>
                        <div class="col-md-3">
                            <label class="form-label fw-bold small text-muted">الخدمة المخصصة</label>
                            <select name="serviceKey" class="form-select fw-bold" required>
                                <% (executorServiceOptions || []).forEach((service) => { %>
                                    <option value="<%= service.key %>"><%= service.label %></option>
                                <% }) %>
                            </select>
                        </div>
                        <div class="col-md-3">
                            <label class="form-label fw-bold small text-muted">الرصيد الافتتاحي</label>
                            <input type="number" step="any" name="balance" class="form-control fw-bold text-center" value="0" dir="ltr">
                        </div>
                        <div class="col-md-3" id="manualReceiptPrefixField">
                            <label class="form-label fw-bold small text-muted">رمز مرجع الإيصال</label>
                            <input type="text" id="manualReceiptPrefixInput" name="manualReceiptPrefix" class="form-control fw-bold text-center" placeholder="مثال: 999" inputmode="numeric" pattern="\d{3}" maxlength="3" dir="ltr">
                            <div class="form-text">3 أرقام، ويُولد تلقائياً عند تركه فارغاً.</div>
                        </div>

                        <div class="col-12" id="humanLoginFields">
                            <div class="p-3 rounded-4 border" style="background: var(--input-bg); border-color: var(--border-glass) !important;">
                                <div class="fw-bold mb-3"><i class="fa-solid fa-user-tie text-success me-1"></i> حساب مسؤول المنفذ</div>
                                <div class="row g-3">
                                    <div class="col-md-3">
                                        <label class="form-label fw-bold small text-muted">اسم المسؤول</label>
                                        <input type="text" name="managerName" class="form-control human-required" placeholder="الاسم الثلاثي" autocomplete="name" minlength="3" required>
                                    </div>
                                    <div class="col-md-3">
                                        <label class="form-label fw-bold small text-muted">رقم الهاتف</label>
                                        <input type="tel" name="managerPhone" class="form-control human-required" placeholder="0910000000" autocomplete="tel" inputmode="tel" dir="ltr" required>
                                    </div>
                                    <div class="col-md-3">
                                        <label class="form-label fw-bold small text-muted">اسم الدخول</label>
                                        <div class="input-group" dir="ltr">
                                            <input type="text" name="webUsername" class="form-control human-required" placeholder="executor_1" autocomplete="username" pattern="[A-Za-z0-9_]+" minlength="3" required>
                                            <span class="input-group-text">@ahram.com</span>
                                        </div>
                                    </div>
                                    <div class="col-md-3">
                                        <label class="form-label fw-bold small text-muted">كلمة المرور</label>
                                        <input type="password" name="webPassword" class="form-control human-required" placeholder="6 أحرف على الأقل" autocomplete="new-password" minlength="6" required>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="col-12 d-none" id="apiFields">
                            <div class="p-3 rounded-4 border" style="background: rgba(245, 158, 11, 0.08); border-color: rgba(245, 158, 11, 0.35) !important;">
                                <div class="fw-bold mb-3 text-warning"><i class="fa-solid fa-plug-circle-bolt me-1"></i> بيانات API الخاصة بهذا المنفذ</div>
                                <div class="row g-3">
                                    <div class="col-md-6">
                                        <label class="form-label fw-bold small text-muted">مزود الخدمة</label>
                                        <select name="apiProviderKey" id="apiProviderKey" class="form-select fw-bold">
                                            <% (typeof apiProviderPresets !== 'undefined' ? apiProviderPresets : []).forEach(provider => { %>
                                                <option value="<%= provider.key %>"
                                                        data-api-url="<%= provider.apiUrl %>"
                                                        data-service-id="<%= provider.serviceId %>"
                                                        data-provider-id="<%= provider.providerId %>"
                                                        data-field-id="<%= provider.fieldId %>"
                                                        data-machine-serial="<%= provider.machineSerial %>">
                                                    <%= provider.nameAr || provider.name %>
                                                </option>
                                            <% }) %>
                                        </select>
                                    </div>
                                    <div class="col-md-6">
                                        <label class="form-label fw-bold small text-muted">رابط API</label>
                                        <input type="text" name="apiUrl" class="form-control" placeholder="https://zaynpay.com" value="https://zaynpay.com" dir="ltr">
                                    </div>
                                    <div class="col-md-3">
                                        <label class="form-label fw-bold small text-muted">اسم مستخدم API</label>
                                        <input type="text" name="apiUsername" class="form-control" dir="ltr">
                                    </div>
                                    <div class="col-md-3">
                                        <label class="form-label fw-bold small text-muted">كلمة مرور API</label>
                                        <input type="password" name="apiPassword" class="form-control" dir="ltr">
                                    </div>
                                    <div class="col-md-6">
                                        <label class="form-label fw-bold small text-muted">API Token / Secret</label>
                                        <input type="text" name="apiToken" class="form-control" dir="ltr">
                                    </div>
                                    <div class="col-md-2">
                                        <label class="form-label fw-bold small text-muted">Service ID</label>
                                        <input type="number" name="apiServiceId" class="form-control text-center" value="85" dir="ltr">
                                    </div>
                                    <div class="col-md-2">
                                        <label class="form-label fw-bold small text-muted">Current Provider ID</label>
                                        <input type="number" name="apiProviderId" class="form-control text-center" value="16" dir="ltr">
                                    </div>
                                    <div class="col-md-2">
                                        <label class="form-label fw-bold small text-muted">Field ID</label>
                                        <input type="number" name="apiFieldId" class="form-control text-center" value="5488" dir="ltr">
                                    </div>
                                    <div class="col-md-3">
                                        <label class="form-label fw-bold small text-muted">Machine Serial</label>
                                        <input type="text" name="apiMachineSerial" class="form-control text-center" value="XP1" dir="ltr">
                                    </div>
                                    <div class="col-md-9 d-flex align-items-end">
                                        <div class="text-muted small">يمكن إضافة أكثر من منفذ API، وكل منفذ سيستخدم بياناته عند توجيه الطلب إليه.</div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="modal-footer border-0 pt-0">
                    <button type="button" class="btn btn-light fw-bold px-4" data-bs-dismiss="modal">إلغاء</button>
                    <button type="submit" class="btn btn-primary fw-bold px-4">
                        <i class="fa-solid fa-floppy-disk me-1"></i> إنشاء المنفذ
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

<script>
    function toggleSidebar() { 
        document.querySelector('.sidebar').classList.toggle('active');
        document.querySelector('.sidebar-overlay').classList.toggle('active'); 
    }

    function toggleExecutorApiFields() {
        const typeField = document.getElementById('botType');
        const apiFields = document.getElementById('apiFields');
        const humanFields = document.getElementById('humanLoginFields');
        const receiptPrefixField = document.getElementById('manualReceiptPrefixField');
        const receiptPrefixInput = document.getElementById('manualReceiptPrefixInput');
        if (!typeField || !apiFields || !humanFields) return;
        const isApi = typeField.value === 'api';
        apiFields.classList.toggle('d-none', !isApi);
        humanFields.classList.toggle('d-none', isApi);
        receiptPrefixField?.classList.toggle('d-none', isApi);
        if (receiptPrefixInput) receiptPrefixInput.disabled = isApi;
        humanFields.querySelectorAll('.human-required').forEach((input) => {
            input.required = !isApi;
            input.disabled = isApi;
        });
    }

    async function copyExecutorLink(inputId, successMessage) {
        const input = document.getElementById(inputId);
        if (!input) return;
        try {
            await navigator.clipboard.writeText(input.value);
            Swal.fire({ icon: 'success', title: successMessage, timer: 1300, showConfirmButton: false });
        } catch (_) {
            input.focus();
            input.select();
            document.execCommand('copy');
            Swal.fire({ icon: 'success', title: successMessage, timer: 1300, showConfirmButton: false });
        }
    }

    function applyApiProviderPreset() {
        const providerField = document.getElementById('apiProviderKey');
        if (!providerField) return;
        const selected = providerField.options[providerField.selectedIndex];
        if (!selected) return;
        const setValue = (name, value) => {
            const input = document.querySelector(`[name="${name}"]`);
            if (input && value) input.value = value;
        };
        setValue('apiUrl', selected.dataset.apiUrl);
        setValue('apiServiceId', selected.dataset.serviceId);
        setValue('apiProviderId', selected.dataset.providerId);
        setValue('apiFieldId', selected.dataset.fieldId);
        setValue('apiMachineSerial', selected.dataset.machineSerial);
    }

    function escapeHtml(value) {
        return String(value ?? '')
            .replace(/&/g, '&amp;')
            .replace(/</g, '&lt;')
            .replace(/>/g, '&gt;')
            .replace(/"/g, '&quot;')
            .replace(/'/g, '&#039;');
    }

    async function testApiExecutor(button) {
        const botId = button?.dataset?.apiBotId;
        if (!botId) return;
        const originalHtml = button.innerHTML;
        button.disabled = true;
        button.innerHTML = '<i class="fa-solid fa-spinner fa-spin me-2"></i> جاري اختبار الرصيد...';

        try {
            const res = await fetch(`/executor/${encodeURIComponent(botId)}/test-api`, {
                method: 'POST',
                headers: {
                    'Accept': 'application/json',
                    'Content-Type': 'application/json',
                    'x-csrf-token': csrfToken
                },
                body: '{}'
            });
            const data = await res.json().catch(() => ({}));
            if (!res.ok || !data.success) {
                throw new Error(data.message || data.error || 'فشل اختبار منفذ API');
            }

            Swal.fire({
                icon: 'success',
                title: 'منفذ API يعمل بنجاح',
                html: `
                    <div style="direction:rtl;text-align:right;line-height:1.9">
                        <div><b>رصيد الخدمة:</b> <span dir="ltr">${Number(data.serviceCredit || 0).toFixed(2)}</span></div>
                        <div><b>الرصيد النقدي:</b> <span dir="ltr">${Number(data.cashCredit || 0).toFixed(2)}</span></div>
                        <div><b>المتاح:</b> <span dir="ltr">${Number(data.availableBalance || data.balance || 0).toFixed(2)}</span></div>
                    </div>
                `,
                confirmButtonText: 'تمام'
            }).then(() => window.location.reload());
        } catch (error) {
            Swal.fire({
                icon: 'error',
                title: 'فشل اختبار منفذ API',
                html: `<div style="direction:rtl;text-align:right">${escapeHtml(error.message)}</div>`,
                confirmButtonText: 'إغلاق'
            });
        } finally {
            button.disabled = false;
            button.innerHTML = originalHtml;
        }
    }

    async function archiveExecutor(button) {
        const executorId = button?.dataset?.executorId;
        const executorName = button?.dataset?.executorName || 'المنفذ';
        const executorStatus = button?.dataset?.executorStatus;
        if (!executorId) return;

        if (executorStatus === 'active') {
            await Swal.fire({
                icon: 'info',
                title: 'أوقف المنفذ أولاً',
                text: 'ادخل إلى ملف المنفذ وأوقف استقبال العمليات، ثم ارجع لتنقل الحساب إلى الأرشيف.',
                confirmButtonText: 'فهمت'
            });
            return;
        }

        const confirmation = await Swal.fire({
            icon: 'warning',
            title: 'نقل الحساب إلى الأرشيف؟',
            text: `سيتم تعطيل حساب ${executorName} وجميع موظفيه، مع الاحتفاظ بكل العمليات والتقارير المالية.`,
            input: 'textarea',
            inputLabel: 'سبب الحذف أو الأرشفة',
            inputPlaceholder: 'مثال: الحساب غير نشط منذ فترة',
            inputAttributes: { maxlength: '500' },
            showCancelButton: true,
            confirmButtonColor: '#dc2626',
            cancelButtonColor: '#64748b',
            confirmButtonText: 'نعم، احذف الحساب',
            cancelButtonText: 'تراجع',
            reverseButtons: true
        });
        if (!confirmation.isConfirmed) return;

        const originalHtml = button.innerHTML;
        button.disabled = true;
        button.innerHTML = '<i class="fa-solid fa-spinner fa-spin"></i>';
        try {
            const response = await fetch(`/executor/${encodeURIComponent(executorId)}/archive`, {
                method: 'POST',
                headers: {
                    'Accept': 'application/json',
                    'Content-Type': 'application/json',
                    'x-csrf-token': csrfToken
                },
                body: JSON.stringify({ reason: confirmation.value || '' })
            });
            const data = await response.json().catch(() => ({}));
            if (!response.ok || !data.success) throw new Error(data.message || 'تعذر أرشفة حساب المنفذ.');

            await Swal.fire({
                icon: 'success',
                title: 'تم نقل الحساب إلى الأرشيف',
                text: data.message,
                confirmButtonText: 'فتح الأرشيف'
            });
            window.location.assign(data.redirectUrl || '/executors?tab=archive');
        } catch (error) {
            await Swal.fire({
                icon: 'error',
                title: 'لم يتم حذف الحساب',
                text: error.message,
                confirmButtonText: 'إغلاق'
            });
            button.disabled = false;
            button.innerHTML = originalHtml;
        }
    }

    document.addEventListener('DOMContentLoaded', () => {
        const typeField = document.getElementById('botType');
        if (typeField) typeField.addEventListener('change', toggleExecutorApiFields);
        toggleExecutorApiFields();
        const providerField = document.getElementById('apiProviderKey');
        if (providerField) providerField.addEventListener('change', applyApiProviderPreset);
        applyApiProviderPreset();

        const shouldOpenCreateModal = <%- JSON.stringify(typeof query !== 'undefined' && query.openCreate ? true : false) %>;
        const createModalEl = document.getElementById('createExecutorModal');
        if (shouldOpenCreateModal && createModalEl && window.bootstrap) {
            new bootstrap.Modal(createModalEl).show();
        }
    });
</script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
