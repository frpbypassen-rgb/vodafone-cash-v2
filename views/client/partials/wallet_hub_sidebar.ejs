<%# LEGACY_GATED: the CLIENT OS sidebar is retired. All customer pages use wallet_hub_sidebar_hub. %>
<%- include('wallet_hub_sidebar_hub', {
    user: typeof user !== 'undefined' ? user : (typeof account !== 'undefined' ? account : {}),
    activePage: typeof activePage !== 'undefined' ? activePage : 'home',
    isSystemOpen: typeof isSystemOpen !== 'undefined' ? isSystemOpen : true
}) %>
