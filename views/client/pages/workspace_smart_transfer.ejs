<% if (workspace.isCompany) { %>
<section class="cos-ledger-office">
    <div>
        <span class="bw-section-kicker">لصق الرسالة</span>
        <h2>التحويل الذكي</h2>
        <p>الصق رسالة التحويل، راجع البيانات في نافذة سريعة، ثم أرسل العملية مباشرة بعد التأكيد.</p>
    </div>
    <div class="cos-ledger-links">
        <a href="/client/services" class="bw-button ghost"><i class="fa-solid fa-cubes"></i>معرض الخدمات</a>
    </div>
</section>
<% } else { %>
<p class="cos-isolated-note">الصق رسالة التحويل، راجع البيانات في نافذة سريعة، ثم أرسل العملية مباشرة بعد التأكيد.</p>
<% } %>
<%- include('../partials/smart_transfer_panel', { smartTransferActionLabel: 'معاينة التحويل' }) %>
