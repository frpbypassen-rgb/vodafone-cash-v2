<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <meta http-equiv="Cache-Control" content="no-cache, no-store, must-revalidate">
    <meta http-equiv="Pragma" content="no-cache">
    <title>منصة التنفيذ | الأهرام</title>
    
    <script>
        const savedTheme = localStorage.getItem('ahram_theme') || 'light';
        document.documentElement.setAttribute('data-theme', savedTheme);
        document.documentElement.setAttribute('data-bs-theme', savedTheme);
    </script>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.rtl.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;500;700;800;900&family=JetBrains+Mono:wght@700&display=swap" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script>window.executorCsrfToken = <%- JSON.stringify(typeof csrfToken !== 'undefined' ? csrfToken : '') %>;</script>
    <script src="/js/executor-api.js"></script>
    
    <style>
        /* 🎨 المتغيرات (الوضع النهاري) */
        :root { 
            --bg-body: #f0f4f8;
            --bg-gradient: linear-gradient(135deg, #f0f4f8 0%, #e2e8f0 50%, #f0f4f8 100%);
            --glass-bg: rgba(255, 255, 255, 0.92);
            --glass-border: rgba(0, 0, 0, 0.06);
            --text-main: #0f172a;
            --text-muted: #64748b;
            --accent-gold: #d4af37;
            --accent-blue: #0ea5e9;
            --accent-green: #10b981;
            --accent-red: #ef4444;
            --shadow-glass: 0 8px 32px 0 rgba(31, 38, 135, 0.06);
            --shadow-card: 0 4px 20px rgba(0,0,0,0.04);
            --card-hover: translateY(-4px);
            --nav-gradient: linear-gradient(135deg, rgba(255,255,255,0.95) 0%, rgba(241,245,249,0.95) 100%);
        }

        /* 🌙 المتغيرات (الوضع الليلي) */
        [data-theme="dark"] {
            --bg-body: #0c1222;
            --bg-gradient: linear-gradient(135deg, #0c1222 0%, #131d35 50%, #0c1222 100%);
            --glass-bg: rgba(22, 33, 55, 0.88);
            --glass-border: rgba(255, 255, 255, 0.07);
            --text-main: #f1f5f9;
            --text-muted: #8899b0;
            --shadow-glass: 0 8px 32px 0 rgba(0, 0, 0, 0.35);
            --shadow-card: 0 4px 20px rgba(0,0,0,0.2);
            --accent-gold: #fbbf24;
            --accent-blue: #38bdf8;
            --nav-gradient: linear-gradient(135deg, rgba(22,33,55,0.95) 0%, rgba(15,23,42,0.95) 100%);
        }

        * { box-sizing: border-box; }
        body { background: var(--bg-gradient); background-attachment: fixed; color: var(--text-main); font-family: 'Tajawal', sans-serif; transition: background 0.4s, color 0.3s; padding-bottom: 80px; overflow-x: hidden; min-height: 100vh; }

        ::-webkit-scrollbar { width: 5px; }
        ::-webkit-scrollbar-track { background: transparent; }
        ::-webkit-scrollbar-thumb { background: rgba(100,116,139,0.3); border-radius: 10px; }
        ::-webkit-scrollbar-thumb:hover { background: rgba(100,116,139,0.5); }

        /* 🛸 Navbar Glassmorphism */
        .navbar-glass {
            background: var(--nav-gradient); backdrop-filter: blur(20px) saturate(1.2); -webkit-backdrop-filter: blur(20px) saturate(1.2);
            border-bottom: 1px solid var(--glass-border); padding: 14px 24px; position: sticky; top: 0; z-index: 1000;
            box-shadow: 0 1px 20px rgba(0,0,0,0.06);
            display: flex; justify-content: space-between; align-items: center; gap: 16px; margin-bottom: 24px; transition: 0.3s;
        }
        [data-theme="dark"] .navbar-glass { box-shadow: 0 1px 20px rgba(0,0,0,0.25); }

        .executor-info { display: flex; align-items: center; gap: 12px; }
        .executor-avatar { 
            width: 44px; height: 44px; border-radius: 14px;
            background: linear-gradient(135deg, rgba(212,175,55,0.15), rgba(212,175,55,0.05));
            color: var(--accent-gold); display: flex; align-items: center; justify-content: center; font-size: 1.3rem;
            border: 1px solid rgba(212,175,55,0.25); transition: 0.3s;
        }
        .executor-avatar:hover { transform: scale(1.08); }
        .executor-name { font-weight: 900; font-size: 1.05rem; margin: 0; color: var(--text-main); letter-spacing: -0.3px; }
        .executor-role { font-size: 0.72rem; color: var(--text-muted); font-weight: 700; }

        .nav-actions { display: flex; gap: 6px; align-items: center; }
        .nav-btn { 
            background: transparent; border: 1px solid var(--glass-border); color: var(--text-main);
            padding: 7px 14px; border-radius: 10px; font-weight: 700; text-decoration: none;
            transition: all 0.25s ease; display: inline-flex; align-items: center; justify-content: center; gap: 5px; font-size: 0.85rem;
            font-family: 'Tajawal', sans-serif;
        }
        .nav-btn:hover { background: rgba(14,165,233,0.08); border-color: rgba(14,165,233,0.25); transform: translateY(-1px); color: var(--accent-blue); }
        .nav-btn.active { background: rgba(14,165,233,0.1); border-color: rgba(14,165,233,0.3); color: var(--accent-blue); }
        .btn-logout { background: rgba(239,68,68,0.06); color: var(--accent-red); border-color: rgba(239,68,68,0.2); }
        .btn-logout:hover { background: var(--accent-red); color: white; border-color: var(--accent-red); }
        .btn-theme { padding: 7px 11px; border-radius: 50%; color: var(--accent-gold); font-size: 0.95rem; }
        .btn-theme:hover { background: rgba(251,191,36,0.12); transform: rotate(20deg) translateY(-1px); }
        
        /* 📡 Radar Animation */
        .radar-box { display: flex; align-items: center; gap: 12px; }
        .pulse-dot { width: 10px; height: 10px; background-color: var(--accent-green); border-radius: 50%; position: relative; flex-shrink: 0; }
        .pulse-dot::after { content: ''; position: absolute; width: 100%; height: 100%; top: 0; left: 0; background-color: var(--accent-green); border-radius: 50%; animation: radarPulse 1.5s infinite; z-index: -1; }
        @keyframes radarPulse { 0% { transform: scale(1); opacity: 0.7; } 100% { transform: scale(3.5); opacity: 0; } }

        /* ⚡ Task Cards */
        @keyframes cardSlideIn { from { opacity: 0; transform: translateY(15px); } to { opacity: 1; transform: translateY(0); } }
        .task-card { 
            background: var(--glass-bg); backdrop-filter: blur(14px); border: 1px solid var(--glass-border);
            border-radius: 18px; padding: 20px; transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
            box-shadow: var(--shadow-card); height: 100%; display: flex; flex-direction: column;
            position: relative; overflow: hidden; animation: cardSlideIn 0.4s ease-out;
        }
        
        .task-card::before { content: ''; position: absolute; top: 0; right: 0; width: 4px; height: 100%; background: var(--accent-gold); transition: 0.3s; border-radius: 0 0 0 4px; }
        .task-card:hover { transform: var(--card-hover); box-shadow: 0 12px 30px rgba(0,0,0,0.08); border-color: rgba(212,175,55,0.25); }
        [data-theme="dark"] .task-card:hover { box-shadow: 0 12px 30px rgba(0,0,0,0.35); border-color: rgba(255,255,255,0.12); }
        
        [data-theme="dark"] .task-mine { background: linear-gradient(145deg, rgba(16,185,129,0.07) 0%, var(--glass-bg) 100%); border-color: rgba(16,185,129,0.25); }
        [data-theme="light"] .task-mine { background: linear-gradient(145deg, rgba(16,185,129,0.04) 0%, rgba(255,255,255,0.95) 100%); border-color: rgba(16,185,129,0.25); }
        .task-mine::before { background: var(--accent-green); box-shadow: 0 0 12px var(--accent-green); }
        
        .task-locked { opacity: 0.55; filter: grayscale(35%); }
        .task-locked::before { background: var(--text-muted); }

        .task-header { display: flex; justify-content: space-between; align-items: flex-start; margin-bottom: 14px; border-bottom: 1px dashed var(--glass-border); padding-bottom: 10px; }
        .task-id { font-size: 1rem; font-weight: 900; color: var(--text-muted); font-family: 'JetBrains Mono', monospace; }
        .cyber-badge { padding: 4px 10px; border-radius: 8px; font-size: 0.72rem; font-weight: 800; display: inline-flex; align-items: center; gap: 4px; letter-spacing: 0.2px; }
        
        .badge-pending { background: rgba(212,175,55,0.1); color: var(--accent-gold); border: 1px solid rgba(212,175,55,0.2); }
        .badge-mine { background: rgba(16,185,129,0.1); color: var(--accent-green); border: 1px solid rgba(16,185,129,0.2); }
        .badge-locked { background: rgba(100,116,139,0.08); color: #7a8da6; border: 1px solid rgba(100,116,139,0.15); }

        .data-label { font-size: 0.78rem; color: var(--text-muted); font-weight: 700; margin-bottom: 5px; display: block; }
        
        .copy-box { background: rgba(0,0,0,0.02); border: 1px solid var(--glass-border); padding: 10px 14px; border-radius: 12px; cursor: pointer; transition: all 0.2s ease; display: flex; justify-content: space-between; align-items: center; margin-bottom: 14px; }
        [data-theme="dark"] .copy-box { background: rgba(255,255,255,0.025); }
        .copy-box:hover { border-color: var(--accent-blue); background: rgba(14,165,233,0.06); }
        .copy-box:active { transform: scale(0.97); }
        
        .copy-val { font-size: 1.15rem; font-weight: 900; color: var(--text-main); font-family: 'JetBrains Mono', Tahoma, sans-serif; letter-spacing: 1px; word-break: break-all; }
        .copy-val.amount { color: var(--accent-green); font-size: 1.4rem; }
        .copy-icon { color: var(--text-muted); font-size: 0.95rem; transition: 0.2s; }
        .copy-box:hover .copy-icon { color: var(--accent-blue); }

        .tx-notes { background: rgba(212,175,55,0.04); border-right: 3px solid var(--accent-gold); padding: 8px 12px; border-radius: 8px; font-size: 0.8rem; font-weight: 700; color: var(--text-main); margin-bottom: 14px; line-height: 1.5; }
        .tx-beneficiary { background: rgba(14,165,233,0.04); border-right: 3px solid var(--accent-blue); padding: 8px 12px; border-radius: 8px; font-size: 0.8rem; font-weight: 700; color: var(--text-main); margin-bottom: 14px; line-height: 1.5; }

        /* 🔘 منطقة الأزرار السفلية */
        .action-zone { margin-top: auto; padding-top: 14px; border-top: 1px dashed var(--glass-border); display: flex; flex-direction: column; gap: 8px; }
        
        .btn-cyber-main { background: linear-gradient(135deg, #10b981, #059669); color: white; border: none; padding: 12px; border-radius: 12px; font-weight: 800; font-size: 0.95rem; transition: all 0.3s ease; display: flex; align-items: center; justify-content: center; gap: 8px; box-shadow: 0 4px 12px rgba(16,185,129,0.25); cursor: pointer; width: 100%; font-family: 'Tajawal', sans-serif; }
        .btn-cyber-main:hover { transform: translateY(-2px); box-shadow: 0 6px 18px rgba(16,185,129,0.35); filter: brightness(1.05); color: white; }

        .horizontal-actions { display: flex; flex-direction: row; gap: 6px; width: 100%; }
        
        .btn-cyber-sub { flex: 1; padding: 10px 5px; border-radius: 10px; font-weight: 800; font-size: 0.82rem; background: rgba(0,0,0,0.02); border: 1px solid var(--glass-border); color: var(--text-main); transition: all 0.2s ease; display: flex; align-items: center; justify-content: center; gap: 5px; cursor: pointer; white-space: nowrap; overflow: hidden; font-family: 'Tajawal', sans-serif; }
        [data-theme="dark"] .btn-cyber-sub { background: rgba(255,255,255,0.03); }
        
        .btn-cyber-sub.btn-warning-neon:hover { background: rgba(245,158,11,0.08); border-color: rgba(245,158,11,0.25); color: #d97706; }
        [data-theme="dark"] .btn-cyber-sub.btn-warning-neon:hover { color: #fcd34d; border-color: rgba(251,191,36,0.35); }

        .btn-cyber-sub.btn-info-neon:hover { background: rgba(14,165,233,0.08); border-color: rgba(14,165,233,0.25); color: #0284c7; }
        [data-theme="dark"] .btn-cyber-sub.btn-info-neon:hover { color: #7dd3fc; border-color: rgba(56,189,248,0.35); }

        .btn-cyber-sub.btn-danger-neon:hover { background: rgba(239,68,68,0.08); border-color: rgba(239,68,68,0.25); color: #dc2626; }
        [data-theme="dark"] .btn-cyber-sub.btn-danger-neon:hover { color: #fca5a5; border-color: rgba(248,113,113,0.35); }

        .btn-accept-start { background: linear-gradient(135deg, #0ea5e9, #2563eb); color: white; width: 100%; padding: 12px; border-radius: 12px; font-weight: 800; border: none; box-shadow: 0 4px 12px rgba(14,165,233,0.25); transition: all 0.3s ease; font-family: 'Tajawal', sans-serif; font-size: 0.95rem; }
        .btn-accept-start:hover { transform: translateY(-2px); box-shadow: 0 6px 18px rgba(14,165,233,0.35); color: white; }

        /* 📱 شاشات الهواتف الذكية */
        @media (max-width: 768px) {
            body {
                padding-top: 75px !important;
                padding-bottom: 90px !important;
            }
            .navbar-glass {
                position: fixed !important;
                top: 0;
                left: 0;
                right: 0;
                z-index: 1050;
                margin-bottom: 0 !important;
                flex-direction: row !important;
                justify-content: space-between !important;
                align-items: center !important;
                padding: 10px 16px !important;
                border-radius: 0 0 16px 16px !important;
                box-shadow: 0 4px 20px rgba(0,0,0,0.15) !important;
            }
            .executor-info {
                width: auto !important;
                justify-content: flex-start !important;
                gap: 8px !important;
            }
            .executor-avatar {
                width: 36px !important;
                height: 36px !important;
                border-radius: 10px !important;
                font-size: 1.1rem !important;
            }
            .executor-name {
                font-size: 0.9rem !important;
            }
            .executor-role {
                display: none !important;
            }
            .nav-actions {
                width: auto !important;
                justify-content: flex-end !important;
                gap: 6px !important;
            }
            .nav-btn {
                padding: 6px 10px !important;
                font-size: 0.8rem !important;
                border-radius: 8px !important;
                flex: none !important;
            }
            .nav-btn span {
                display: none !important;
            }
            .task-card { padding: 16px; border-radius: 16px; }
            .copy-val { font-size: 1.05rem; }
            .copy-val.amount { font-size: 1.25rem; }
            .copy-box { padding: 8px 12px; }
            .btn-text-hide { display: none; }
            .btn-cyber-sub { padding: 10px; font-size: 0.95rem; } 

            /* 📱 شريط التنقل السفلي للهواتف */
            .bottom-nav-mobile {
                position: fixed;
                bottom: 0;
                left: 0;
                right: 0;
                height: 64px;
                background: var(--nav-gradient);
                backdrop-filter: blur(20px) saturate(1.2);
                -webkit-backdrop-filter: blur(20px) saturate(1.2);
                border-top: 1px solid var(--glass-border);
                display: flex;
                justify-content: space-around;
                align-items: center;
                z-index: 1050;
                box-shadow: 0 -4px 16px rgba(0, 0, 0, 0.08);
                padding-bottom: env(safe-area-inset-bottom);
            }
            .bottom-nav-item {
                display: flex;
                flex-direction: column;
                align-items: center;
                justify-content: center;
                gap: 4px;
                color: var(--text-muted);
                text-decoration: none;
                flex: 1;
                height: 100%;
                transition: all 0.2s ease;
                font-size: 0.72rem;
                font-weight: 700;
            }
            .bottom-nav-item i {
                font-size: 1.25rem;
                transition: transform 0.2s ease;
            }
            .bottom-nav-item:hover {
                color: var(--text-main);
            }
            .bottom-nav-item.active {
                color: var(--accent-gold);
            }
            .bottom-nav-item.active i {
                transform: translateY(-2px);
            }
        }

        @media (min-width: 769px) {
            .btn-text-hide { display: inline; }
        }

        /* 🚨 نظام الطوارئ الشامل والمودال */
        #emergencyOverlay { position: fixed; top: 0; left: 0; width: 100vw; height: 100vh; background: rgba(220,38,38,0.95); z-index: 999999; display: none; flex-direction: column; justify-content: center; align-items: center; text-align: center; backdrop-filter: blur(15px); animation: redAlert 1s infinite alternate; }
        @keyframes redAlert { 0% { background: rgba(220,38,38,0.95); } 100% { background: rgba(153,27,27,0.98); } }
        .shake-icon { font-size: 7rem; color: white; margin-bottom: 20px; animation: shake 0.5s infinite; filter: drop-shadow(0 0 20px rgba(255,255,255,0.8)); }
        @keyframes shake { 0%, 100% { transform: translateX(0) rotate(0); } 25% { transform: translateX(-15px) rotate(-15deg); } 75% { transform: translateX(15px) rotate(15deg); } }

        .modal-content { background: var(--glass-bg); backdrop-filter: blur(20px); border: 1px solid var(--glass-border); border-radius: 22px; color: var(--text-main); box-shadow: var(--shadow-glass); }
        .modal-header { border-bottom: 1px dashed var(--glass-border); }
        .form-control { background: rgba(0,0,0,0.04); border: 1px solid var(--glass-border); color: var(--text-main); border-radius: 12px; padding: 12px; font-weight: bold; transition: 0.2s; }
        [data-theme="dark"] .form-control { background: rgba(255,255,255,0.04); }
        .form-control:focus { background: transparent; border-color: var(--accent-blue); box-shadow: 0 0 0 3px rgba(14,165,233,0.1); color: var(--text-main); }
        
        .drop-zone { border: 2px dashed rgba(16,185,129,0.35); border-radius: 16px; padding: 25px; text-align: center; cursor: pointer; transition: 0.3s; background: rgba(16,185,129,0.03); }
        .drop-zone:hover { border-color: var(--accent-green); background: rgba(16,185,129,0.08); }
        .preview-badge-remove { position: absolute; top: -5px; left: -5px; width: 22px; height: 22px; background: var(--accent-red); color: white; border-radius: 50%; display: flex; align-items: center; justify-content: center; border: none; cursor: pointer; box-shadow: 0 3px 8px rgba(239,68,68,0.35); z-index: 10; font-size: 10px; }

        /* 🌟 Empty State */
        .empty-state { text-align: center; padding: 60px 20px; }
        .empty-state i { font-size: 3.5rem; margin-bottom: 16px; opacity: 0.2; color: var(--text-muted); }
        .empty-state p { color: var(--text-muted); font-weight: 700; font-size: 1rem; }
        @keyframes floatIcon { 0%, 100% { transform: translateY(0); } 50% { transform: translateY(-8px); } }
        .empty-state i { animation: floatIcon 3s ease-in-out infinite; }

        /* SweetAlert */
        .swal2-popup { background: var(--glass-bg) !important; backdrop-filter: blur(15px); color: var(--text-main) !important; border: 1px solid var(--glass-border); border-radius: 20px !important; }
        .swal2-title, .swal2-html-container { color: var(--text-main) !important; }
        .swal2-input, .swal2-textarea { background: rgba(0,0,0,0.04) !important; color: var(--text-main) !important; border-color: var(--glass-border) !important; border-radius: 10px !important; }
        [data-theme="dark"] .swal2-input, [data-theme="dark"] .swal2-textarea { background: rgba(255,255,255,0.04) !important; }
    </style>
    <link rel="stylesheet" href="/css/brand-ui.css?v=20260804-brand1">
    <link rel="stylesheet" href="/css/executor-workspace.css?v=20260824-2030">
    <link rel="stylesheet" href="/css/executor-2030-override.css?v=20260824-2030">
    <link rel="stylesheet" href="/css/executor-os.css?v=20260824-2030">
</head>
<body class="executor-os executor-portal-v2 executor-page-dashboard">

<%- include('partials/navigation', { emp, activePage: 'tasks' }) %>

<div id="emergencyOverlay">
    <i class="fa-solid fa-radiation shake-icon"></i>
    <h1 class="text-white fw-900 mb-3 px-3" style="font-size: 2.5rem; text-shadow: 0 4px 10px rgba(0,0,0,0.5);">🚨 إنذار طارئ! 🚨</h1>
    <h4 id="emergencyMessage" class="text-white fw-bold mb-5 px-4" style="line-height: 1.8; max-width: 700px; text-shadow: 0 2px 5px rgba(0,0,0,0.5);">يوجد طلب متأخر يجب سحبه فوراً!</h4>
    <button onclick="dismissEmergency(event)" class="btn btn-light fw-900 px-5 py-4 fs-3 text-danger shadow-lg" style="border-radius: 50px; border: 4px solid white; transition: 0.2s;">
        <i class="fa-solid fa-volume-xmark me-3"></i> إيقـاف الإنـذار 🔇
    </button>
</div>

<script>
    const myExecutorId = '<%= emp._id %>';
    const isExecutorManager = <%= emp.role === 'manager' ? 'true' : 'false' %>;
</script>

<nav class="navbar-glass">
    <div class="executor-info">
        <div class="executor-avatar"><i class="fa-solid fa-user-astronaut"></i></div>
        <div>
            <h4 class="executor-name"><%= emp.name %></h4>
            <span class="executor-role"><i class="fa-solid fa-network-wired me-1"></i> غرفة العمليات: <%= emp.groupId?.name || 'بدون غرفة' %></span>
        </div>
    </div>
    <div class="nav-actions">
        <% if (emp.role === 'manager') { %><span class="nav-btn text-success border border-success border-opacity-25" style="cursor: default; font-weight: 800;" title="المبلغ المتاح"><i class="fa-solid fa-wallet me-1"></i> <%= Number(emp.groupId?.balance || 0).toLocaleString('en-US', { maximumFractionDigits: 0 }) %> ج.م</span><% } %>
        <button class="nav-btn btn-theme shadow-sm" onclick="toggleTheme()" title="المظهر"><i class="fa-solid fa-moon" id="themeIcon"></i></button>
        <a href="/executor-portal/logout" class="nav-btn btn-logout"><i class="fa-solid fa-power-off"></i></a>
    </div>
</nav>

<div class="container-fluid px-3 px-md-4 mt-2 mb-5">
    <div id="radarSection">
        <header class="executor-live-heading">
            <div>
                <h1>غرفة التنفيذ</h1>
                <p>تصل المهام الجديدة فورًا، ويظهر رقم المستلم كاملًا بعد قبول المهمة.</p>
            </div>
            <div class="d-flex align-items-center gap-2">
                <% if (emp.role === 'manager') { %>
                    <a href="/executor-portal/settings" class="nav-btn" title="إعدادات توزيع المهام"><i class="fa-solid fa-route"></i><span class="d-none d-md-inline"> التوجيه</span></a>
                <% } %>
                <span class="executor-live-status"><i></i> المراقبة نشطة</span>
            </div>
        </header>

        <div class="executor-task-layout">
            <section class="executor-task-stage">
                <div id="tasksList" class="row g-3"></div>
            </section>

            <aside class="executor-monitor-rail" aria-label="مراقبة غرفة العمليات">
                <section class="executor-monitor-panel">
                    <div>
                        <div class="executor-monitor-visual"><img src="/images/executor-3d-alert.png" alt="رادار مراقبة العمليات"></div>
                        <strong>الرادار يعمل</strong>
                        <small>متصل بغرفة العمليات لحظة بلحظة</small>
                    </div>
                </section>
                <section class="executor-day-panel">
                    <h2>ملخص اليوم</h2>
                    <div class="executor-day-stat"><span>تم التنفيذ</span><strong id="monitorCompletedCount">0</strong></div>
                    <div class="executor-day-stat"><span>إجمالي القيمة</span><strong id="monitorCompletedAmount">0 ج.م</strong></div>
                    <div class="executor-day-stat"><span>قيد التنفيذ</span><strong id="monitorActiveCount" class="is-live">0</strong></div>
                </section>
            </aside>
        </div>
    </div>

    <!-- 👤 قسم حسابي -->
    <div id="profileSection" style="display: none;">
        <div class="row g-3 g-xl-4">
            <!-- كارت الملف الشخصي والتعريف -->
            <div class="col-12 col-lg-4">
                <div class="p-4" style="background: var(--glass-bg); backdrop-filter: blur(14px); border: 1px solid var(--glass-border); border-radius: 20px; box-shadow: var(--shadow-card); text-align: center;">
                    <div class="mx-auto mb-3" style="width: 80px; height: 80px; border-radius: 22px; background: linear-gradient(135deg, rgba(212,175,55,0.15), rgba(212,175,55,0.05)); display: flex; align-items: center; justify-content: center; border: 1px solid rgba(212,175,55,0.25);">
                        <i class="fa-solid fa-user-astronaut fs-1 text-warning"></i>
                    </div>
                    <h4 class="fw-bold mb-1" style="color: var(--text-main);"><%= emp.name %></h4>
                    <span class="badge bg-warning bg-opacity-10 text-warning px-3 py-1.5 rounded-pill mb-4" style="font-size: 0.8rem; border: 1px solid rgba(251,191,36,0.2);">
                        <%= emp.role === 'manager' ? 'مدير النظام' : 'منفذ العمليات' %>
                    </span>
                    
                    <div class="text-start border-top pt-3" style="border-top-style: dashed !important; border-color: var(--glass-border) !important; display: flex; flex-direction: column; gap: 12px;">
                        <div class="d-flex justify-content-between align-items-center">
                            <span class="text-muted small"><i class="fa-regular fa-envelope me-1"></i> اسم المستخدم:</span>
                            <span class="fw-bold" style="color: var(--text-main);"><%= emp.webUsername %></span>
                        </div>
                        <div class="d-flex justify-content-between align-items-center">
                            <span class="text-muted small"><i class="fa-solid fa-network-wired me-1"></i> غرفة العمليات:</span>
                            <span class="fw-bold" style="color: var(--text-main);"><%= emp.groupId?.name || 'بدون غرفة' %></span>
                        </div>
                        <div class="d-flex justify-content-between align-items-center">
                            <span class="text-muted small"><i class="fa-solid fa-phone me-1"></i> رقم الهاتف:</span>
                            <span class="fw-bold" style="color: var(--text-main); font-family: monospace;"><%= emp.phone || '---' %></span>
                        </div>
                        <% if (emp.telegramId) { %>
                        <div class="d-flex justify-content-between align-items-center">
                            <span class="text-muted small"><i class="fa-brands fa-telegram me-1"></i> معرف التليجرام:</span>
                            <span class="fw-bold" style="color: var(--text-main); font-family: monospace;"><%= emp.telegramId %></span>
                        </div>
                        <% } %>
                        <div class="d-flex justify-content-between align-items-center">
                            <span class="text-muted small"><i class="fa-solid fa-circle-check me-1"></i> الحالة:</span>
                            <span class="badge bg-success bg-opacity-10 text-success border border-success border-opacity-25 py-1 px-2.5">نشط</span>
                        </div>
                    </div>
                </div>
            </div>

            <!-- كارت إدارة العهدة والإحصائيات -->
            <div class="col-12 col-lg-8">


                <!-- إحصائيات اليوم -->
                <div class="row g-3 mb-4">
                    <div class="col-6">
                        <div class="p-4 text-center" style="background: var(--glass-bg); backdrop-filter: blur(14px); border: 1px solid var(--glass-border); border-radius: 20px; box-shadow: var(--shadow-card); height: 100%;">
                            <div class="mb-2 text-success"><i class="fa-solid fa-check-double fs-1"></i></div>
                            <span class="text-muted small fw-bold">العمليات المنفذة اليوم</span>
                            <h2 id="completedCount" class="fw-bold mt-2 text-main m-0">0</h2>
                        </div>
                    </div>
                    <div class="col-6">
                        <div class="p-4 text-center" style="background: var(--glass-bg); backdrop-filter: blur(14px); border: 1px solid var(--glass-border); border-radius: 20px; box-shadow: var(--shadow-card); height: 100%;">
                            <div class="mb-2 text-primary"><i class="fa-solid fa-sack-dollar fs-1"></i></div>
                            <span class="text-muted small fw-bold">إجمالي المبالغ اليوم</span>
                            <h2 id="completedSum" class="fw-bold mt-2 text-success m-0" style="font-family: 'JetBrains Mono', monospace; font-size: 1.8rem;">0 <span class="fs-6 fw-normal text-muted">EGP</span></h2>
                        </div>
                    </div>
                </div>

                <!-- العمليات المنفذة اليوم -->
                <div class="p-4" style="background: var(--glass-bg); backdrop-filter: blur(14px); border: 1px solid var(--glass-border); border-radius: 20px; box-shadow: var(--shadow-card);">
                    <h5 class="fw-bold mb-3 text-main"><i class="fa-solid fa-clock-rotate-left text-muted me-1"></i> تفاصيل العمليات المنفذة اليوم</h5>
                    <div id="completedTasksList" style="max-height: 380px; overflow-y: auto; display: flex; flex-direction: column; gap: 10px; padding-left: 5px;">
                        <!-- dynamic items -->
                        <div class="text-center text-muted py-4"><i class="fa-solid fa-satellite-dish fs-3 mb-2 opacity-50"></i><p class="m-0 small">لا يوجد عمليات منفذة اليوم بعد</p></div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="modal fade" id="uploadModal" tabindex="-1">
    <div class="modal-dialog modal-dialog-centered px-2">
        <div class="modal-content">
            <div class="modal-header border-0 pb-0 pt-4 px-4">
                <h5 class="modal-title fw-bold" style="color: var(--accent-green);"><i class="fa-solid fa-cloud-arrow-up me-2"></i> توثيق وإنهاء العملية</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" onclick="resetUploadModal()" style="filter: invert(0.5);"></button>
            </div>
            <div class="modal-body p-4">
                <div id="dropZone" class="drop-zone mb-3">
                    <i class="fa-solid fa-images fs-2 mb-2" style="color: var(--accent-green); filter: drop-shadow(0 0 10px rgba(16,185,129,0.3));"></i>
                    <h6 class="mb-1 fw-bold" id="proofRequirementTitle">إرفاق الصورة اختياري</h6>
                    <p class="text-muted small m-0" id="proofRequirementHint" style="font-size: 0.75rem;">يمكنك إرفاق صور متعددة، أو المتابعة لإنشاء إيصال نظام تلقائي</p>
                    <input type="file" id="proofInput" class="d-none" accept="image/*" multiple>
                </div>
                
                <div id="previewsGrid" class="row g-2 mb-3"></div>

                <div class="mb-4">
                    <label class="data-label mb-2"><i class="fa-solid fa-fingerprint me-1"></i> رقم التنفيذ (اختياري)</label>
                    <input type="text" id="senderPhoneInput" class="form-control fw-bold text-center val-phone" placeholder="01108172258 أو 2258 أو 899" inputmode="numeric" maxlength="11" dir="ltr">
                    <div class="form-text text-muted">يظهر في الإيصال بصيغة مخفية، بينما رقم هاتف العميل يظهر كاملاً.</div>
                </div>
                
                <button id="btnFinish" class="btn-cyber-main w-100 m-0">
                    <i class="fa-solid fa-check-double me-2"></i> تأكيد التنفيذ والإرسال <kbd class="ms-2 bg-dark bg-opacity-25 border-0 text-white px-2 py-1 rounded" style="font-size: 0.75rem; font-family: sans-serif;">Enter ↵</kbd>
                </button>
            </div>
        </div>
    </div>
</div>

<audio id="sirenSound" src="https://assets.mixkit.co/active_storage/sfx/991/991-preview.mp3" loop preload="auto"></audio>
<audio id="alertSound" src="https://assets.mixkit.co/active_storage/sfx/2869/2869-preview.mp3" preload="auto"></audio>

<!-- شريط التنقل السفلي للهواتف (App-like Bottom Navigation) -->
<div class="bottom-nav-mobile d-md-none">
    <!-- حسابي (الملف الشخصي والمنجزات) -->
    <a href="#profile" onclick="switchSection('profile')" class="bottom-nav-item" id="nav-profile">
        <i class="fa-solid fa-circle-user"></i>
        <span>حسابي</span>
    </a>
    <!-- الرادار -->
    <a href="#radar" onclick="switchSection('radar')" class="bottom-nav-item active" id="nav-dash">
        <i class="fa-solid fa-satellite-dish"></i>
        <span>الرادار</span>
    </a>
    <!-- التقارير -->
    <a href="/executor-portal/reports" class="bottom-nav-item" id="nav-rep">
        <i class="fa-solid fa-chart-line"></i>
        <span>التقارير</span>
    </a>
    <% if (emp.role === 'manager') { %>
    <!-- الموظفين -->
    <a href="/executor-portal/employees" class="bottom-nav-item" id="nav-emp">
        <i class="fa-solid fa-users"></i>
        <span>الموظفين</span>
    </a>
    <% } %>
</div>

<script src="/js/executor-workspace.js?v=20260819-1"></script>
<script>
    // 🔀 Section Toggling Logic
    function switchSection(sectionId) {
        const radarSec = document.getElementById('radarSection');
        const profileSec = document.getElementById('profileSection');
        
        // Top nav buttons
        const topNavRadar = document.getElementById('top-nav-radar');
        const topNavProfile = document.getElementById('top-nav-profile');
        
        // Bottom nav buttons
        const botNavRadar = document.getElementById('nav-dash');
        const botNavProfile = document.getElementById('nav-profile');
        
        // De-activate all
        topNavRadar?.classList.remove('active');
        topNavProfile?.classList.remove('active');
        botNavRadar?.classList.remove('active');
        botNavProfile?.classList.remove('active');
        
        if (sectionId === 'profile') {
            if (radarSec) radarSec.style.display = 'none';
            if (profileSec) profileSec.style.display = 'block';
            
            topNavProfile?.classList.add('active');
            botNavProfile?.classList.add('active');
        } else {
            if (radarSec) radarSec.style.display = 'block';
            if (profileSec) profileSec.style.display = 'none';
            
            topNavRadar?.classList.add('active');
            botNavRadar?.classList.add('active');
        }
    }

    function handleHashChange() {
        const hash = window.location.hash;
        if (hash === '#profile') {
            switchSection('profile');
        } else {
            switchSection('radar');
        }
    }

    window.addEventListener('hashchange', handleHashChange);
    document.addEventListener('DOMContentLoaded', handleHashChange);
    // 🌓 Theme Logic
    function toggleTheme() {
        const html = document.documentElement;
        const currentTheme = html.getAttribute('data-theme');
        const newTheme = currentTheme === 'dark' ? 'light' : 'dark';
        html.setAttribute('data-theme', newTheme);
        html.setAttribute('data-bs-theme', newTheme);
        localStorage.setItem('ahram_theme', newTheme);
        document.getElementById('themeIcon').className = newTheme === 'dark' ? 'fa-solid fa-sun text-warning' : 'fa-solid fa-moon text-dark';
    }
    document.addEventListener('DOMContentLoaded', () => {
        const theme = localStorage.getItem('ahram_theme') || 'light';
        document.getElementById('themeIcon').className = theme === 'dark' ? 'fa-solid fa-sun text-warning' : 'fa-solid fa-moon text-dark';
    });

    let activeTasksIds = [];
    let currentCompletingId = null; 
    let currentCompletingTransferType = '';
    let currentEmergencyId = null; 
    let selectedFilesArray = [];
    let lastTasksHash = '';
    // 🚀 ذاكرة ذكية تمنع إعادة عرض أي إنذار تم إيقافه محلياً
    let dismissedAlertsIds = new Set();
    
    // 📤 Upload Logic
    const dropZone = document.getElementById('dropZone'); const proofInput = document.getElementById('proofInput'); const previewsGrid = document.getElementById('previewsGrid');
    dropZone.onclick = () => proofInput.click();
    dropZone.ondragover = (e) => { e.preventDefault(); dropZone.style.borderColor = 'var(--accent-green)'; dropZone.style.background = 'rgba(16, 185, 129, 0.1)'; };
    dropZone.ondragleave = (e) => { e.preventDefault(); dropZone.style.borderColor = 'rgba(16, 185, 129, 0.4)'; dropZone.style.background = 'rgba(16, 185, 129, 0.05)'; };
    dropZone.ondrop = (e) => { e.preventDefault(); dropZone.style.borderColor = 'rgba(16, 185, 129, 0.4)'; dropZone.style.background = 'rgba(16, 185, 129, 0.05)'; if (e.dataTransfer.files.length) handleIncomingFiles(e.dataTransfer.files); };
    proofInput.onchange = (e) => { if (e.target.files.length) handleIncomingFiles(e.target.files); };
    window.addEventListener('paste', e => {
        if(document.getElementById('uploadModal').classList.contains('show')) {
            const items = (e.clipboardData || e.originalEvent.clipboardData).items; let pastedFiles = [];
            for (let index in items) { const item = items[index]; if (item.kind === 'file' && item.type.startsWith('image/')) pastedFiles.push(item.getAsFile()); }
            if(pastedFiles.length > 0) handleIncomingFiles(pastedFiles);
        }
    });

    // ⌨️ دعم الضغط على زر Enter لتأكيد الإرسال
    document.addEventListener('keydown', function(e) {
        if (e.key === 'Enter') {
            if (document.body.classList.contains('swal2-shown')) return; 
            const uploadModal = document.getElementById('uploadModal');
            if (uploadModal && uploadModal.classList.contains('show')) {
                e.preventDefault(); 
                const btnFinish = document.getElementById('btnFinish');
                if (btnFinish && !btnFinish.disabled) {
                    btnFinish.click();
                }
            }
        }
    });

    function handleIncomingFiles(files) {
        const remainingSlots = Math.max(0, 5 - selectedFilesArray.length);
        if (remainingSlots === 0) {
            Swal.fire('تنبيه', 'يمكن إرفاق خمس صور إثبات كحد أقصى.', 'info');
            return;
        }
        const acceptedFiles = Array.from(files).slice(0, remainingSlots);
        for (let i = 0; i < acceptedFiles.length; i++) {
            if (!acceptedFiles[i].type.startsWith('image/')) { Swal.fire('خطأ', 'يرجى اختيار صور فقط.', 'error'); continue; }
            selectedFilesArray.push(acceptedFiles[i]);
        }
        if (files.length > remainingSlots) Swal.fire('تنبيه', 'تمت إضافة الصور المتاحة فقط. الحد الأقصى خمس صور.', 'info');
        renderImagesGrid();
    }
    
    function renderImagesGrid() {
        previewsGrid.innerHTML = '';
        if (selectedFilesArray.length === 0) return;
        selectedFilesArray.forEach((file, index) => {
            const reader = new FileReader();
            reader.onload = (e) => {
                const gridCol = document.createElement('div'); gridCol.className = 'col-4 position-relative mb-2';
                gridCol.innerHTML = `
                    <div class="position-relative rounded-3 overflow-hidden shadow-sm" style="height: 80px; border: 1px solid var(--glass-border);">
                        <img src="${e.target.result}" style="width: 100%; height: 100%; object-fit: cover;">
                        <button type="button" onclick="removeImageFromList(${index})" class="preview-badge-remove"><i class="fa-solid fa-xmark"></i></button>
                    </div>`;
                previewsGrid.appendChild(gridCol);
            };
            reader.readAsDataURL(file);
        });
    }

    function removeImageFromList(index) { selectedFilesArray.splice(index, 1); renderImagesGrid(); }
    function updateProofRequirement() {
        const title = document.getElementById('proofRequirementTitle');
        const hint = document.getElementById('proofRequirementHint');
        if (title) title.textContent = 'إرفاق الصور اختياري';
        if (hint) hint.textContent = 'يمكنك إرفاق حتى خمس صور، أو المتابعة لإنشاء إيصال نظام تلقائي';
        dropZone.classList.remove('border-danger');
    }
    function resetUploadModal() {
        selectedFilesArray = [];
        proofInput.value = '';
        previewsGrid.innerHTML = '';
        document.getElementById('senderPhoneInput').value = '';
        updateProofRequirement();
    }

    function compressSingleImagePromise(file) {
        return new Promise((resolve) => {
            const reader = new FileReader();
            reader.onload = function(event) {
                const img = new Image();
                img.onload = function() {
                    const canvas = document.createElement('canvas'); const MAX_WIDTH = 1200; const MAX_HEIGHT = 1600;
                    let width = img.width; let height = img.height;
                    if (width > height) { if (width > MAX_WIDTH) { height *= MAX_WIDTH / width; width = MAX_WIDTH; } } 
                    else { if (height > MAX_HEIGHT) { width *= MAX_HEIGHT / height; height = MAX_HEIGHT; } }
                    canvas.width = width; canvas.height = height; const ctx = canvas.getContext('2d'); ctx.drawImage(img, 0, 0, width, height);
                    resolve(canvas.toDataURL('image/jpeg', 0.6));
                }
                img.src = event.target.result;
            }
            reader.readAsDataURL(file);
        });
    }

    // 📋 النسخ الذكي
    function copyText(text, element) {
        if (!text || text === '---') return;
        if (navigator.clipboard && window.isSecureContext) navigator.clipboard.writeText(text);
        else { let t = document.createElement("textarea"); t.value = text; t.style.position = "fixed"; t.style.left = "-9999px"; document.body.appendChild(t); t.focus(); t.select(); try { document.execCommand('copy'); } catch (err) {} t.remove(); }
        
        const originalHtml = element.innerHTML;
        element.style.borderColor = "var(--accent-green)";
        element.style.background = "rgba(16, 185, 129, 0.1)";
        element.innerHTML = '<b class="val-phone text-success m-auto"><i class="fa-solid fa-check"></i> نُسخ!</b>';
        setTimeout(() => { element.innerHTML = originalHtml; element.style.borderColor = ""; element.style.background = ""; }, 1000);
    }

    const escapeTaskHtml = (value) => String(value ?? '')
        .replace(/&/g, '&amp;')
        .replace(/</g, '&lt;')
        .replace(/>/g, '&gt;')
        .replace(/"/g, '&quot;')
        .replace(/'/g, '&#039;');

    // 📡 جلب البيانات وفرز الكروت الديناميكي (Smart Sorting FIFO)
    let tasksRefreshInFlight = false;
    let manualTaskRoutingEnabled = false;

    function renderManualRoutingControl() {
        const toggle = document.getElementById('manualRoutingToggle');
        const hint = document.getElementById('manualRoutingHint');
        if (!toggle || !hint) return;
        if (manualTaskRoutingEnabled) {
            toggle.className = 'btn btn-primary fw-bold rounded-3 px-3';
            toggle.innerHTML = '<i class="fa-solid fa-toggle-on me-1"></i> مفعل';
            hint.textContent = 'المهام الجديدة لا تظهر للموظفين قبل توجيهها إلى منفذ محدد.';
        } else {
            toggle.className = 'btn btn-outline-primary fw-bold rounded-3 px-3';
            toggle.innerHTML = '<i class="fa-solid fa-toggle-off me-1"></i> تفعيل';
            hint.textContent = 'عند تفعيله لا تظهر المهام للموظفين قبل توجيهها بالاسم.';
        }
    }

    async function toggleManualRouting() {
        if (!isExecutorManager) return;
        const toggle = document.getElementById('manualRoutingToggle');
        if (toggle) toggle.disabled = true;
        try {
            const response = await executorApiFetch('/executor-portal/api/task-routing-mode', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ enabled: !manualTaskRoutingEnabled })
            });
            const data = await readExecutorApiResponse(response);
            manualTaskRoutingEnabled = Boolean(data.manualTaskRoutingEnabled);
            renderManualRoutingControl();
            await refreshTasks();
        } catch (error) {
            Swal.fire('تعذر تحديث التوجيه', error.message, 'error');
        } finally {
            if (toggle) toggle.disabled = false;
        }
    }

    async function routeTask(id) {
        try {
            const candidatesResponse = await executorApiFetch('/executor-portal/api/route-candidates');
            const candidatesData = await readExecutorApiResponse(candidatesResponse);
            const candidates = Array.isArray(candidatesData.employees) ? candidatesData.employees : [];
            if (candidates.length === 0) {
                throw new Error('لا يوجد موظف تنفيذ نشط يمكن توجيه العملية إليه.');
            }
            const options = Object.fromEntries(candidates.map((employee) => [employee._id, `${employee.name} - ${employee.webUsername}`]));
            const { value: employeeId } = await Swal.fire({
                title: 'توجيه العملية',
                text: 'اختر المنفذ الذي ستظهر له العملية ليقبلها وينفذها.',
                input: 'select',
                inputOptions: options,
                inputPlaceholder: 'اختر المنفذ',
                showCancelButton: true,
                confirmButtonText: 'توجيه',
                cancelButtonText: 'إلغاء',
                confirmButtonColor: '#0ea5e9',
                inputValidator: (value) => value ? undefined : 'اختر المنفذ أولاً.'
            });
            if (!employeeId) return;
            const response = await executorApiFetch('/executor-portal/api/route-task/' + id, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ employeeId })
            });
            const result = await readExecutorApiResponse(response);
            await Swal.fire('تم التوجيه', `أصبحت العملية ظاهرة للمنفذ: ${result.employee?.name || ''}`, 'success');
            await refreshTasks();
        } catch (error) {
            Swal.fire('تعذر توجيه العملية', error.message, 'error');
        }
    }

    async function refreshTasks() {
        if (document.hidden || tasksRefreshInFlight) return;
        tasksRefreshInFlight = true;
        try {
            const res = await executorApiFetch('/executor-portal/api/live-tasks');
            const data = await readExecutorApiResponse(res);
            manualTaskRoutingEnabled = Boolean(data.manualTaskRoutingEnabled);
            renderManualRoutingControl();
            
            // 📊 تحديث إحصائيات المعاملات المنجزة اليوم في صفحة حسابي
            if (data.completedToday) {
                const summary = data.completedTodaySummary || {};
                const hasServerSummary = Number.isFinite(Number(summary.amount));
                const count = Number.isFinite(Number(summary.count)) ? Number(summary.count) : data.completedToday.length;
                let sum = hasServerSummary ? Number(summary.amount) : 0;
                let listHtml = '';
                
                data.completedToday.forEach(tx => {
                    const txAmt = parseFloat(tx.amount) || 0;
                    if (!hasServerSummary) sum += txAmt;
                    
                    let typeBadge = '';
                    if (tx.transferType === 'post_account') {
                        typeBadge = '<span class="badge bg-warning bg-opacity-10 text-warning border border-warning border-opacity-25" style="font-size: 0.72rem;"><i class="fa-solid fa-envelope me-1"></i> بريد</span>';
                    } else if (tx.transferType === 'post_card') {
                        typeBadge = '<span class="badge bg-info bg-opacity-10 text-info border border-info border-opacity-25" style="font-size: 0.72rem;"><i class="fa-solid fa-id-card me-1"></i> بطاقة</span>';
                    } else {
                        typeBadge = '<span class="badge bg-danger bg-opacity-10 text-danger border border-danger border-opacity-25" style="font-size: 0.72rem;"><i class="fa-solid fa-mobile-screen me-1"></i> كاش</span>';
                    }
                    
                    const timeStr = tx.updatedAt ? new Date(tx.updatedAt).toLocaleTimeString('ar-LY', { timeZone: 'Africa/Tripoli', hour: '2-digit', minute: '2-digit', second: '2-digit' }) : '---';
                    
                    listHtml += `
                        <div class="d-flex justify-content-between align-items-center p-2.5 rounded-3 border mb-1" style="background: rgba(128,128,128,0.03); border-color: var(--glass-border) !important;">
                            <div class="d-flex align-items-center gap-2">
                                <div class="rounded-circle d-flex align-items-center justify-content-center" style="width: 32px; height: 32px; background: rgba(16, 185, 129, 0.1); color: var(--accent-green);">
                                    <i class="fa-solid fa-check" style="font-size: 0.85rem;"></i>
                                </div>
                                <div>
                                    <div class="d-flex align-items-center gap-2 flex-wrap">
                                        <span class="fw-bold text-main" style="font-size: 0.85rem;">#${tx.customId}</span>
                                        ${typeBadge}
                                    </div>
                                    <small class="text-muted d-block mt-1" style="font-size: 0.75rem;" dir="ltr">${tx.vodafoneNumber || tx.accountNumber || '---'}</small>
                                </div>
                            </div>
                            <div class="text-end">
                                <span class="fw-bold text-success d-block" style="font-size: 0.95rem; font-family: monospace;">+${tx.amount}</span>
                                <small class="text-muted" style="font-size: 0.7rem;"><i class="fa-regular fa-clock me-1"></i> ${timeStr}</small>
                            </div>
                        </div>
                    `;
                });
                
                const countEl = document.getElementById('completedCount');
                if (countEl) countEl.innerText = count;
                
                const sumEl = document.getElementById('completedSum');
                if (sumEl) sumEl.innerText = sum.toLocaleString('en-US', { maximumFractionDigits: 0 }) + ' ج.م';
                
                const listEl = document.getElementById('completedTasksList');
                if (listEl) {
                    listEl.innerHTML = listHtml || `
                        <div class="text-center text-muted py-4">
                            <i class="fa-solid fa-satellite-dish fs-3 mb-2 opacity-50"></i>
                            <p class="m-0 small">لا يوجد عمليات منفذة اليوم بعد</p>
                        </div>
                    `;
                }
            }

            const monitorSummary = data.completedTodaySummary || {};
            const monitorCount = Number.isFinite(Number(monitorSummary.count))
                ? Number(monitorSummary.count)
                : (Array.isArray(data.completedToday) ? data.completedToday.length : 0);
            const monitorAmount = Number.isFinite(Number(monitorSummary.amount)) ? Number(monitorSummary.amount) : 0;
            const monitorActive = Array.isArray(data.tasks)
                ? data.tasks.filter((task) => task.status === 'accepted').length
                : 0;
            const monitorCountEl = document.getElementById('monitorCompletedCount');
            const monitorAmountEl = document.getElementById('monitorCompletedAmount');
            const monitorActiveEl = document.getElementById('monitorActiveCount');
            if (monitorCountEl) monitorCountEl.textContent = monitorCount.toLocaleString('en-US');
            if (monitorAmountEl) monitorAmountEl.textContent = monitorAmount.toLocaleString('en-US', { maximumFractionDigits: 0 }) + ' ج.م';
            if (monitorActiveEl) monitorActiveEl.textContent = monitorActive.toLocaleString('en-US');
            
            // 🚨 الطوارئ الذكية: تحديد إذا كان الموظف مشغولاً أم لا
            const amIBusy = data.tasks.some(t => t.operatorId === myExecutorId && t.status === 'accepted');
            let targetAlert = null;
            if (data.alerts && data.alerts.length > 0) {
                targetAlert = data.alerts.find(a => {
                    if (dismissedAlertsIds.has(a._id)) return false;
                    if (amIBusy) return a.operatorId === myExecutorId;
                    else return a.status === 'processing';
                });
            }

            if (targetAlert) {
                if (currentEmergencyId !== targetAlert._id) {
                    currentEmergencyId = targetAlert._id;
                    document.getElementById('emergencyMessage').innerHTML = `الطلب رقم: <code style="color:var(--accent-gold);">${targetAlert.customId}</code><br><br>${targetAlert.emergencyAlert}`;
                    document.getElementById('emergencyOverlay').style.display = 'flex';
                    const siren = document.getElementById('sirenSound');
                    siren.volume = 1.0;
                    const playPromise = siren.play();
                    if (playPromise !== undefined) {
                        playPromise.catch(error => { console.warn("المتصفح منع تشغيل الصوت تلقائياً."); });
                    }
                }
            } else {
                document.getElementById('emergencyOverlay').style.display = 'none';
                document.getElementById('sirenSound').pause(); 
                document.getElementById('sirenSound').currentTime = 0; 
                currentEmergencyId = null;
            }

            // 💰 تنبيهات الإيداع
            if (data.depAlerts && data.depAlerts.length > 0) {
                for (const alertTx of data.depAlerts) {
                    const alertData = alertTx.executorWebAlert;
                    await Swal.fire({ title: alertData.type === 'success' ? 'إشعار إيداع' : 'تم الرفض', html: `<div class="fw-bold text-${alertData.type === 'success' ? 'success' : 'danger'}">${alertData.text}</div>`, icon: alertData.type, confirmButtonText: 'حسناً', confirmButtonColor: '#10b981' });
                    await executorApiFetch('/executor-portal/api/clear-dep-alert/' + alertTx._id, { method: 'POST' });
                }
            }

            const container = document.getElementById('tasksList');
            const newTasks = data.tasks.filter(t => !activeTasksIds.includes(t._id));
            if (newTasks.length > 0 && !currentEmergencyId) document.getElementById('alertSound').play().catch(()=>{});
            activeTasksIds = data.tasks.map(t => t._id);
            
            // 🔒 فحص الـ Hash لمنع إعادة الرسم والوميض
            const currentTasksHash = JSON.stringify(data.tasks);
            if (currentTasksHash !== lastTasksHash) {
                lastTasksHash = currentTasksHash;
                
                // 🚀 الفرز الذكي (Smart Sorting FIFO) - نظام الطابور المطور
                let myTasks = [];
                let pendingTasks = [];
                let othersTasks = [];

                data.tasks.forEach(t => {
                    if (t.operatorId === myExecutorId && t.status === 'accepted') myTasks.push(t);
                    else if (t.status === 'processing') pendingTasks.push(t); 
                    else othersTasks.push(t); 
                });
                
                const smartSortedTasks = [...myTasks, ...pendingTasks, ...othersTasks];
                let html = '';
                
                smartSortedTasks.forEach(t => {
                    const isMine = t.operatorId === myExecutorId;
                    const acceptedByOther = t.status === 'accepted' && !isMine;
                    const canRoute = isExecutorManager && manualTaskRoutingEnabled && t.status === 'processing';
                    const recipientIsVisible = t.recipientRevealed === true && isMine;
                    const recipientDisplay = t.recipientNumber || t.recipientPrefix || '---';
                    const encodedRecipient = encodeURIComponent(recipientDisplay);
                    const recipientLabel = recipientIsVisible ? 'الرقم / الحساب:' : 'بادئة الرقم / الحساب:';
                    const formattedAmount = Number(t.amount || 0).toLocaleString('en-US', { maximumFractionDigits: 0 });

                    let cardClass = isMine ? 'task-mine' : (acceptedByOther ? 'task-locked' : 'task-pending');
                    let badgeClass = isMine ? 'badge-mine' : (acceptedByOther ? 'badge-locked' : 'badge-pending');
                    let badgeText = isMine ? '<i class="fa-solid fa-bolt me-1"></i> معك' : (acceptedByOther ? '<i class="fa-solid fa-lock me-1"></i> لزميل' : '<i class="fa-solid fa-circle-dot fa-fade me-1"></i> متاح');

                    const taskTypeName = t.transferType === 'post_account' ? 'تحويل بريد حساب' :
                        (t.transferType === 'post_card' ? 'تحويل بريد بطاقة' : 'تحويل محفظة كاش');
                    const taskTypeIcon = t.transferType === 'post_account' ? 'fa-envelope' :
                        (t.transferType === 'post_card' ? 'fa-id-card' : 'fa-mobile-screen-button');

                    let displayNote = t.notes || '';
                    displayNote = displayNote.split('--- سجل الـ API')[0].trim();
                    displayNote = displayNote.split(/\r?\n/).map(line => line.trim()).filter(line => {
                        if (!line) return false;
                        if (/رقم المحول|رقم المرسل|الرقم المرجعي|مرجع|reference|ref/i.test(line)) return true;
                        return !(/^سبب الرفض:|^\[تم |^\[فشل |^\[معلقة |^\[رقم الإلغاء:|^تحويل رصيد صادر إلى|^تحويل رصيد وارد من/.test(line));
                    }).join('\n');
                    const safeDisplayNote = escapeTaskHtml(displayNote).replace(/\n/g, '<br>');
                    const safeAccountName = escapeTaskHtml(t.accountName || '').replace(/\n/g, '<br>');

                    const taskArrivalAt = t.executorReceivedAt || t.createdAt;
                    html += `
                        <div class="col-12 col-xxl-6">
                            <div class="task-card ${cardClass}">
                                <div class="executor-task-card-body">
                                    <div class="task-header">
                                        <div class="executor-task-title">
                                            <div class="executor-task-icon"><i class="fa-solid ${taskTypeIcon}"></i></div>
                                            <div><h2>${taskTypeName}</h2><span>#${escapeTaskHtml(t.customId)}</span></div>
                                        </div>
                                        <span class="cyber-badge ${badgeClass}">${badgeText}</span>
                                    </div>

                                    <div class="executor-task-data">
                                      <div class="executor-task-data-cell">
                                        <span class="data-label">${recipientLabel}</span>
                                        <div class="copy-box" ${recipientIsVisible ? `onclick="copyText(decodeURIComponent('${encodedRecipient}'), this)"` : ''}>
                                            <span class="copy-val" dir="ltr">${escapeTaskHtml(recipientDisplay)}</span>
                                            <i class="fa-solid ${recipientIsVisible ? 'fa-copy' : 'fa-lock'} copy-icon"></i>
                                        </div>
                                      </div>
                                      <div class="executor-task-data-cell">
                                        <span class="data-label">القيمة المطلوبة</span>
                                        <div class="copy-box" onclick="copyText('${t.amount}', this)">
                                            <span class="copy-val amount">${formattedAmount} <small>ج.م</small></span>
                                            <i class="fa-regular fa-copy copy-icon"></i>
                                        </div>
                                      </div>
                                      <div class="executor-task-data-cell">
                                        <span class="data-label">وقت الانتظار</span>
                                        <span class="live-timer" data-time="${taskArrivalAt}">0s</span>
                                      </div>
                                    </div>

                                    ${safeDisplayNote ? `<div class="tx-notes"><i class="fa-solid fa-comment-dots me-1 opacity-50"></i> ${safeDisplayNote}</div>` : ''}
                                    ${safeAccountName && t.transferType !== 'vodafone' ? `<div class="tx-beneficiary"><i class="fa-solid fa-user me-1 opacity-50"></i> ${safeAccountName}</div>` : ''}
                                    ${recipientIsVisible ? '' : '<div class="executor-task-privacy"><i class="fa-solid fa-shield-halved"></i><span>يظهر الرقم كاملًا بعد قبول المهمة للمنفذ الذي سحبها فقط.</span></div>'}
                                </div>
                                
                                <div class="action-zone border-0">
                                    ${canRoute ? `<button onclick="routeTask('${t._id}')" class="btn-accept-start"><i class="fa-solid fa-route me-1"></i> توجيه إلى منفذ</button>` :
                                      acceptedByOther ? `<div class="p-2 rounded-3 text-center fw-bold text-muted border w-100 small" style="background: rgba(0,0,0,0.05);"><i class="fa-solid fa-user-gear me-1"></i> يُنفذ بواسطة: ${escapeTaskHtml(t.executorName || t.assignedExecutorName || 'منفذ آخر')}</div>` :
                                      isMine ? `
                                         <button onclick="openUpload('${t._id}', '${t.transferType || ''}')" class="btn-cyber-main mb-1"><i class="fa-solid fa-check-double"></i> إنهاء العملية</button>
                                         <div class="horizontal-actions">
                                            <button onclick="editAmount('${t._id}')" class="btn-cyber-sub btn-warning-neon" title="تعديل المبلغ"><i class="fa-solid fa-pen-to-square"></i> <span class="btn-text-hide ms-1">تعديل</span></button>
                                            <button onclick="returnTask('${t._id}')" class="btn-cyber-sub btn-info-neon" title="إرجاع الطلب للإدارة"><i class="fa-solid fa-reply"></i> <span class="btn-text-hide ms-1">إرجاع</span></button>
                                            <button onclick="cancelTask('${t._id}')" class="btn-cyber-sub btn-danger-neon" title="إلغاء لخطأ في الرقم"><i class="fa-solid fa-xmark"></i> <span class="btn-text-hide ms-1">إلغاء</span></button>
                                         </div>
                                        ` : `<button onclick="acceptTask('${t._id}')" class="btn-accept-start"><i class="fa-solid fa-bolt me-1"></i> اسحب ونفذ فوراً</button>`
                                    }
                                </div>
                            </div>
                        </div>`;
                });
                container.innerHTML = html || `<div class="col-12"><div class="executor-empty-state-live"><div><img src="/images/executor-3d-alert.png" alt="في انتظار عمليات جديدة"><h2>غرفة العمليات هادئة</h2><p>سيظهر الطلب هنا فور وصول عملية جديدة.</p></div></div></div>`;
            }
        } catch(e) {
        } finally {
            tasksRefreshInFlight = false;
        }
    }

    // ⏱️ تحديث العدادات الزمنية بشكل حي في الشاشة
    setInterval(() => {
        document.querySelectorAll('.live-timer').forEach(el => {
            const createdTime = new Date(el.getAttribute('data-time')).getTime();
            const diffMs = Date.now() - createdTime;
            const diffSec = Math.floor(diffMs / 1000);
            
            if (diffSec >= 0) {
                let text = '';
                if (diffSec < 60) text = `${diffSec} ثانية`;
                else { const m = Math.floor(diffSec / 60); const s = diffSec % 60; text = `${m} دقيقة و ${s} ث`; }
                el.innerText = text;

                if (diffSec >= 120) { el.style.color = 'var(--accent-red)'; el.classList.add('fa-fade'); } 
                else if (diffSec >= 60) { el.style.color = 'var(--accent-gold)'; el.classList.remove('fa-fade'); } 
                else { el.style.color = 'var(--text-main)'; el.classList.remove('fa-fade'); }
            }
        });
    }, 1000);

    // 🎯 الزر التفاعلي (إيقاف الإنذار)
    async function dismissEmergency(event) {
        if (!currentEmergencyId) return;
        const tempId = currentEmergencyId;
        dismissedAlertsIds.add(tempId);
        
        document.getElementById('sirenSound').pause();
        document.getElementById('sirenSound').currentTime = 0; 
        document.getElementById('emergencyOverlay').style.display = 'none'; 
        currentEmergencyId = null; 

        try { await executorApiFetch('/executor-portal/api/clear-alert/' + tempId, { method: 'POST' }); } catch(e) {}
    }



    async function acceptTask(id) {
        try {
            const response = await executorApiFetch('/executor-portal/api/accept-task/' + id, { method: 'POST' });
            await readExecutorApiResponse(response);
            await refreshTasks();
        } catch (error) {
            Swal.fire('تعذر سحب العملية', error.message, 'error');
            await refreshTasks();
        }
    }
    function openUpload(id, transferType) {
        currentCompletingId = id;
        currentCompletingTransferType = String(transferType || '').toLowerCase();
        resetUploadModal();
        new bootstrap.Modal(document.getElementById('uploadModal')).show();
    }

    document.getElementById('btnFinish').onclick = async function() {
        const executionNumber = document.getElementById('senderPhoneInput').value.trim();
        const executionDigits = executionNumber.replace(/\D/g, '');
        if (executionDigits && !/^(?:01\d{9}|\d{3}|\d{4})$/.test(executionDigits)) {
            Swal.fire('رقم التنفيذ غير صالح', 'أدخل رقم هاتف من 11 رقمًا، أو آخر 3 أو 4 أرقام فقط.', 'warning');
            return;
        }
        const btn = this; btn.disabled = true; btn.innerHTML = '<i class="fa-solid fa-spinner fa-spin"></i> ضغط وإرسال...';
        try {
            const compressedImagesBase64 = [];
            for (let i = 0; i < selectedFilesArray.length; i++) { compressedImagesBase64.push(await compressSingleImagePromise(selectedFilesArray[i])); }
            const res = await executorApiFetch('/executor-portal/api/complete-task/' + currentCompletingId, {
                method: 'POST', headers: {'Content-Type': 'application/json'},
                body: JSON.stringify({ imageBase64: compressedImagesBase64[0], imagesBase64: compressedImagesBase64, executionNumber: executionNumber })
            });
            await readExecutorApiResponse(res);
            window.location.reload();
        } catch(e) {
            Swal.fire('تعذر إنهاء العملية', e.message || 'فشل حفظ الإيصال.', 'error');
            btn.disabled = false;
            btn.innerHTML = '<i class="fa-solid fa-check-double me-2"></i> تأكيد التنفيذ والإرسال <kbd class="ms-2 bg-dark bg-opacity-25 border-0 text-white px-2 py-1 rounded" style="font-size: 0.75rem; font-family: sans-serif;">Enter ↵</kbd>';
        }
    };

    async function editAmount(id) {
        const { value: formValues } = await Swal.fire({
            title: 'تعديل المبلغ', html: '<input id="swal-amount" type="number" class="form-control mb-3 text-center fw-bold" placeholder="المبلغ الجديد"><textarea id="swal-reason" class="form-control" placeholder="السبب..." rows="2"></textarea>',
            focusConfirm: false, showCancelButton: true, confirmButtonText: 'تأكيد وحفظ', confirmButtonColor: '#f59e0b',
            preConfirm: () => {
                const a = document.getElementById('swal-amount').value; const rn = document.getElementById('swal-reason').value;
                if (!a || !rn) { Swal.showValidationMessage('مطلوب إدخال البيانات!'); return false; } return [a, rn];
            }
        });
        if (formValues) {
            try {
                const res = await executorApiFetch('/executor-portal/api/edit-amount/' + id, { method: 'POST', headers: {'Content-Type': 'application/json'}, body: JSON.stringify({ newAmount: formValues[0], reason: formValues[1] }) });
                await readExecutorApiResponse(res);
                Swal.fire('تم', 'تم التعديل بنجاح', 'success').then(refreshTasks);
            } catch (error) { Swal.fire('تعذر تعديل المبلغ', error.message, 'error'); }
        }
    }

    async function cancelTask(id) {
        const { value: reason } = await Swal.fire({
            title: 'إلغاء العملية',
            html: `
                <div class="text-end" dir="rtl">
                    <label for="swal-cancel-reason" class="form-label fw-bold w-100">سبب الإلغاء</label>
                    <select id="swal-cancel-reason" class="swal2-select" style="width: 100%; margin: 0;">
                        <option value="" selected disabled>اختر سبب الإلغاء</option>
                        <option value="لا يوجد محفظة">لا يوجد محفظة</option>
                        <option value="محفظة ليميت">محفظة ليميت</option>
                        <option value="الخدمة متوقفة حاليا">الخدمة متوقفة حاليا</option>
                        <option value="الرقم غير صحيح">الرقم غير صحيح</option>
                        <option value="other">سبب آخر</option>
                    </select>
                    <div id="swal-cancel-other-reason-wrap" class="mt-3" style="display: none;">
                        <label for="swal-cancel-other-reason" class="form-label fw-bold w-100">اكتب السبب</label>
                        <textarea id="swal-cancel-other-reason" class="swal2-textarea" rows="3" placeholder="اكتب سبب الإلغاء..."></textarea>
                    </div>
                </div>`,
            showCancelButton: true,
            confirmButtonText: 'إلغاء نهائي',
            confirmButtonColor: '#ef4444',
            cancelButtonText: 'تراجع',
            focusConfirm: false,
            didOpen: () => {
                const select = document.getElementById('swal-cancel-reason');
                const otherReasonWrap = document.getElementById('swal-cancel-other-reason-wrap');
                const otherReason = document.getElementById('swal-cancel-other-reason');
                select.addEventListener('change', () => {
                    const isOther = select.value === 'other';
                    otherReasonWrap.style.display = isOther ? 'block' : 'none';
                    if (isOther) otherReason.focus();
                });
            },
            preConfirm: () => {
                const selectedReason = document.getElementById('swal-cancel-reason').value;
                const customReason = document.getElementById('swal-cancel-other-reason').value.trim();
                if (!selectedReason) {
                    Swal.showValidationMessage('يرجى اختيار سبب الإلغاء.');
                    return false;
                }
                if (selectedReason === 'other' && !customReason) {
                    Swal.showValidationMessage('يرجى كتابة سبب الإلغاء.');
                    return false;
                }
                return selectedReason === 'other' ? customReason : selectedReason;
            }
        });
        if (reason) {
            try {
                const res = await executorApiFetch('/executor-portal/api/cancel-task/' + id, { method: 'POST', headers: {'Content-Type': 'application/json'}, body: JSON.stringify({ reason }) });
                await readExecutorApiResponse(res);
                await refreshTasks();
            } catch (error) { Swal.fire('تعذر إلغاء العملية', error.message, 'error'); }
        }
    }

    async function returnTask(id) {
        const { value: reason } = await Swal.fire({ title: 'إرجاع للإدارة', input: 'text', inputPlaceholder: 'السبب...', showCancelButton: true, confirmButtonText: 'إرجاع', confirmButtonColor: '#0ea5e9', cancelButtonText: 'تراجع', inputValidator: (val) => { if (!val) return 'مطلوب السبب!'; } });
        if (reason) {
            try {
                const res = await executorApiFetch('/executor-portal/api/return-task/' + id, { method: 'POST', headers: {'Content-Type': 'application/json'}, body: JSON.stringify({ reason }) });
                await readExecutorApiResponse(res);
                await refreshTasks();
            } catch (error) { Swal.fire('تعذر إرجاع العملية', error.message, 'error'); }
        }
    }

    // 🟢 دالة زر عرض لوج الـ API
    function showApiLogBtn(btn) {
        const rawLog = btn.getAttribute('data-log');
        showApiLogFromText(rawLog);
    }

    // 🟢 دالة عرض نافذة السجل المنبثقة للـ API
    function showApiLogFromText(rawNotesEnc) {
        const rawNotes = decodeURIComponent(rawNotesEnc);
        let logContent = rawNotes;
        
        if (rawNotes.includes('--- سجل الـ API (Terminal Log) ---')) {
            logContent = rawNotes.split('--- سجل الـ API (Terminal Log) ---')[1].trim();
        } else if (rawNotes.includes('--- سجل الـ API ---')) {
            logContent = rawNotes.split('--- سجل الـ API ---')[1].trim();
        } else if (rawNotes.includes('--- سجل الـ API')) {
            logContent = rawNotes.split('--- سجل الـ API')[1].trim();
        }
        
        Swal.fire({
            title: '<div style="color:#10b981; font-family:monospace; font-size:1.2rem; display:flex; align-items:center; gap:10px; justify-content:center;"><i class="fa-solid fa-terminal"></i> سجل التخاطب مع السيرفر</div>',
            html: `<div style="background:#0f172a; color:#a5b4fc; padding:15px; border-radius:8px; text-align:left; direction:ltr; font-family:'JetBrains Mono', monospace; font-size:0.85rem; white-space:pre-wrap; max-height:400px; overflow-y:auto; border:1px solid #334155; box-shadow:inset 0 2px 4px rgba(0,0,0,0.5);">${logContent}</div>`,
            background: '#1e293b',
            color: '#fff',
            confirmButtonColor: '#3b82f6',
            confirmButtonText: 'إغلاق الشاشة',
            width: '600px',
            customClass: { popup: 'border border-secondary rounded-4' }
        });
    }

    const refreshVisibleExecutorTasks = () => {
        if (!document.hidden) refreshTasks();
    };
    setInterval(refreshVisibleExecutorTasks, 4000);
    document.addEventListener('visibilitychange', refreshVisibleExecutorTasks);
    refreshVisibleExecutorTasks();
</script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
