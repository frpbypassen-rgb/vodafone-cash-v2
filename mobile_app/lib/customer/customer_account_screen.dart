/// Customer account & identity vault screen.
///
/// Implementation currently lives in [CustomerAccountScreen] inside
/// `app_screens.dart` due to shared dialog dependencies. Import this file
/// instead of `app_screens.dart` when you only need the account screen.
library;

export '../app_screens.dart' show CustomerAccountScreen;
